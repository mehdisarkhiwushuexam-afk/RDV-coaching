<!DOCTYPE html>
<html lang="fr">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>MEHDI SARKHI – Coaching Fitness & Wushu</title>
<link href="https://fonts.googleapis.com/css2?family=Bebas+Neue&family=Barlow:ital,wght@0,300;0,400;0,500;0,600;0,700;1,400&display=swap" rel="stylesheet">
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
<style>
:root {
  --gold: #D4AF37;
  --gold-light: #F0D060;
  --red: #C0392B;
  --red-dark: #922B21;
  --black: #0A0A0A;
  --dark: #111111;
  --dark2: #1A1A1A;
  --dark3: #222222;
  --dark4: #2C2C2C;
  --gray: #888;
  --light: #F5F5F0;
  --white: #FFFFFF;
}

*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

html { scroll-behavior: smooth; }

body {
  background: var(--black);
  color: var(--light);
  font-family: 'Barlow', sans-serif;
  font-weight: 400;
  overflow-x: hidden;
}

/* ─── HERO ─── */
.hero {
  min-height: 100vh;
  background: var(--black);
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  position: relative;
  overflow: hidden;
  padding: 40px 20px;
}

.hero::before {
  content: '';
  position: absolute;
  inset: 0;
  background: 
    radial-gradient(ellipse 80% 60% at 50% 0%, rgba(192,57,43,0.18) 0%, transparent 70%),
    radial-gradient(ellipse 60% 40% at 80% 80%, rgba(212,175,55,0.08) 0%, transparent 60%);
}

.hero-bg-text {
  position: absolute;
  font-family: 'Bebas Neue', cursive;
  font-size: clamp(120px, 25vw, 280px);
  color: rgba(255,255,255,0.025);
  letter-spacing: 0.05em;
  user-select: none;
  white-space: nowrap;
  animation: floatText 8s ease-in-out infinite;
}

@keyframes floatText {
  0%, 100% { transform: translateY(0px); }
  50% { transform: translateY(-20px); }
}

.hero-content {
  position: relative;
  text-align: center;
  z-index: 2;
}

.hero-badge {
  display: inline-block;
  background: rgba(212,175,55,0.15);
  border: 1px solid rgba(212,175,55,0.4);
  color: var(--gold);
  padding: 6px 20px;
  border-radius: 100px;
  font-size: 0.75rem;
  font-weight: 600;
  letter-spacing: 2px;
  text-transform: uppercase;
  margin-bottom: 24px;
  animation: fadeInDown 0.8s ease both;
}

.hero-name {
  font-family: 'Bebas Neue', cursive;
  font-size: clamp(60px, 12vw, 120px);
  line-height: 0.9;
  letter-spacing: 2px;
  animation: fadeInUp 0.8s 0.2s ease both;
}

.hero-name span { color: var(--gold); }

.hero-tagline {
  font-size: clamp(14px, 2vw, 17px);
  color: var(--gray);
  margin: 20px 0 12px;
  font-weight: 300;
  letter-spacing: 1px;
  animation: fadeInUp 0.8s 0.4s ease both;
}

.hero-credentials {
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  gap: 10px;
  margin: 24px 0 40px;
  animation: fadeInUp 0.8s 0.5s ease both;
}

.cred-pill {
  display: flex;
  align-items: center;
  gap: 7px;
  background: var(--dark3);
  border: 1px solid var(--dark4);
  padding: 7px 14px;
  border-radius: 100px;
  font-size: 0.78rem;
  font-weight: 500;
  color: #ccc;
}

.cred-pill i { color: var(--gold); font-size: 0.85rem; }

.hero-cta {
  display: flex;
  gap: 14px;
  justify-content: center;
  flex-wrap: wrap;
  animation: fadeInUp 0.8s 0.6s ease both;
}

.btn-primary {
  background: var(--gold);
  color: var(--black);
  border: none;
  padding: 16px 40px;
  border-radius: 100px;
  font-family: 'Barlow', sans-serif;
  font-size: 0.95rem;
  font-weight: 700;
  letter-spacing: 1px;
  text-transform: uppercase;
  cursor: pointer;
  transition: all 0.3s;
  text-decoration: none;
  display: inline-flex;
  align-items: center;
  gap: 10px;
}

.btn-primary:hover {
  background: var(--gold-light);
  transform: translateY(-3px);
  box-shadow: 0 15px 40px rgba(212,175,55,0.35);
}

.btn-outline-gold {
  background: transparent;
  color: var(--gold);
  border: 1.5px solid var(--gold);
  padding: 15px 36px;
  border-radius: 100px;
  font-family: 'Barlow', sans-serif;
  font-size: 0.95rem;
  font-weight: 600;
  letter-spacing: 1px;
  text-transform: uppercase;
  cursor: pointer;
  transition: all 0.3s;
  text-decoration: none;
  display: inline-flex;
  align-items: center;
  gap: 10px;
}

.btn-outline-gold:hover {
  background: rgba(212,175,55,0.1);
  transform: translateY(-3px);
}

.scroll-indicator {
  position: absolute;
  bottom: 30px;
  left: 50%;
  transform: translateX(-50%);
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 8px;
  color: var(--gray);
  font-size: 0.7rem;
  letter-spacing: 2px;
  text-transform: uppercase;
  animation: bounce 2s infinite;
}

.scroll-indicator i { font-size: 1.2rem; color: var(--gold); }

@keyframes bounce {
  0%, 100% { transform: translateX(-50%) translateY(0); }
  50% { transform: translateX(-50%) translateY(-10px); }
}

/* ─── PHOTOS INSPIRANTES ─── */
.hero-gallery {
  position: relative;
  z-index: 2;
  display: flex;
  gap: 16px;
  justify-content: center;
  flex-wrap: wrap;
  margin: 40px 0 0;
  animation: fadeInUp 0.8s 0.7s ease both;
}

.hero-gallery-item {
  width: 180px;
  height: 240px;
  border-radius: 16px;
  overflow: hidden;
  border: 2px solid var(--dark4);
  transition: all 0.4s cubic-bezier(0.165, 0.84, 0.44, 1);
  position: relative;
  cursor: pointer;
  background: var(--dark2);
  flex-shrink: 0;
}

.hero-gallery-item:hover {
  border-color: var(--gold);
  transform: translateY(-8px);
  box-shadow: 0 20px 40px rgba(212, 175, 55, 0.2);
}

.hero-gallery-item img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  transition: transform 0.6s;
}

.hero-gallery-item:hover img {
  transform: scale(1.05);
}

.hero-gallery-item .gallery-label {
  position: absolute;
  bottom: 0;
  left: 0;
  right: 0;
  background: linear-gradient(transparent, rgba(0,0,0,0.85));
  padding: 20px 12px 10px;
  font-size: 0.72rem;
  font-weight: 700;
  letter-spacing: 1px;
  text-transform: uppercase;
  color: var(--gold);
  text-align: center;
}

@media (max-width: 768px) {
  .hero-gallery {
    gap: 10px;
  }
  .hero-gallery-item {
    width: 120px;
    height: 170px;
  }
}

@media (max-width: 480px) {
  .hero-gallery-item {
    width: 90px;
    height: 130px;
  }
}

/* ─── PACKS ─── */
section { padding: 80px 20px; }

.section-header {
  text-align: center;
  margin-bottom: 60px;
}

.section-label {
  display: inline-block;
  color: var(--gold);
  font-size: 0.75rem;
  font-weight: 700;
  letter-spacing: 3px;
  text-transform: uppercase;
  margin-bottom: 12px;
}

.section-title {
  font-family: 'Bebas Neue', cursive;
  font-size: clamp(40px, 7vw, 72px);
  line-height: 1;
  letter-spacing: 1px;
}

.section-title span { color: var(--gold); }

.section-sub {
  color: var(--gray);
  margin-top: 14px;
  font-size: 1rem;
  font-weight: 300;
}

/* PACKS GRID */
#packs { background: var(--dark); }

.packs-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
  gap: 24px;
  max-width: 1200px;
  margin: 0 auto;
}

.pack-card {
  background: var(--dark2);
  border: 1px solid var(--dark4);
  border-radius: 24px;
  padding: 36px 28px;
  position: relative;
  cursor: pointer;
  transition: all 0.4s cubic-bezier(0.165, 0.84, 0.44, 1);
  overflow: hidden;
}

.pack-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 3px;
  background: var(--dark4);
  transition: background 0.3s;
}

.pack-card.selected,
.pack-card:hover {
  border-color: var(--gold);
  transform: translateY(-8px);
  box-shadow: 0 30px 60px rgba(0,0,0,0.4);
}

.pack-card.selected::before,
.pack-card:hover::before {
  background: var(--gold);
}

.pack-card.featured {
  border-color: rgba(212,175,55,0.4);
}

.pack-card.featured::before { background: var(--gold); }

.pack-badge-featured {
  position: absolute;
  top: 16px;
  right: 16px;
  background: var(--gold);
  color: var(--black);
  font-size: 0.65rem;
  font-weight: 800;
  letter-spacing: 1px;
  text-transform: uppercase;
  padding: 4px 10px;
  border-radius: 100px;
}

.pack-icon {
  width: 52px;
  height: 52px;
  background: rgba(212,175,55,0.1);
  border-radius: 14px;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 1.4rem;
  color: var(--gold);
  margin-bottom: 20px;
}

.pack-name {
  font-family: 'Bebas Neue', cursive;
  font-size: 1.8rem;
  letter-spacing: 1px;
  color: var(--white);
  margin-bottom: 6px;
}

.pack-sessions {
  font-size: 0.8rem;
  color: var(--gray);
  font-weight: 500;
  text-transform: uppercase;
  letter-spacing: 1px;
  margin-bottom: 20px;
}

.pack-price {
  font-family: 'Bebas Neue', cursive;
  font-size: 3.2rem;
  color: var(--gold);
  line-height: 1;
  margin-bottom: 4px;
}

.pack-price span { font-size: 1.6rem; }

.pack-validity {
  font-size: 0.75rem;
  color: var(--gray);
  margin-bottom: 24px;
  font-weight: 300;
}

.pack-features {
  list-style: none;
  margin-bottom: 28px;
}

.pack-features li {
  display: flex;
  align-items: center;
  gap: 10px;
  font-size: 0.85rem;
  color: #bbb;
  padding: 6px 0;
  border-bottom: 1px solid var(--dark4);
}

.pack-features li:last-child { border-bottom: none; }
.pack-features li i { color: var(--gold); font-size: 0.7rem; }

.pack-select-btn {
  width: 100%;
  background: var(--dark4);
  color: var(--white);
  border: 1px solid var(--dark4);
  padding: 13px;
  border-radius: 100px;
  font-family: 'Barlow', sans-serif;
  font-size: 0.85rem;
  font-weight: 700;
  letter-spacing: 1px;
  text-transform: uppercase;
  cursor: pointer;
  transition: all 0.3s;
}

.pack-card.selected .pack-select-btn,
.pack-card:hover .pack-select-btn {
  background: var(--gold);
  color: var(--black);
  border-color: var(--gold);
}

/* ─── HORAIRES ─── */
#horaires { background: var(--black); }

.horaires-container {
  max-width: 900px;
  margin: 0 auto;
}

.days-tabs {
  display: flex;
  gap: 10px;
  flex-wrap: wrap;
  justify-content: center;
  margin-bottom: 40px;
}

.day-tab {
  background: var(--dark2);
  border: 1px solid var(--dark4);
  color: var(--gray);
  padding: 10px 22px;
  border-radius: 100px;
  font-family: 'Barlow', sans-serif;
  font-size: 0.85rem;
  font-weight: 600;
  letter-spacing: 1px;
  text-transform: uppercase;
  cursor: pointer;
  transition: all 0.3s;
}

.day-tab.active {
  background: var(--gold);
  color: var(--black);
  border-color: var(--gold);
  box-shadow: 0 8px 24px rgba(212,175,55,0.3);
}

.day-tab:hover:not(.active) {
  border-color: var(--gold);
  color: var(--gold);
}

.time-slots {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(160px, 1fr));
  gap: 14px;
}

.time-slot {
  background: var(--dark2);
  border: 1px solid var(--dark4);
  border-radius: 16px;
  padding: 20px 16px;
  text-align: center;
  cursor: pointer;
  transition: all 0.3s;
  position: relative;
}

.time-slot.available:hover,
.time-slot.selected {
  border-color: var(--gold);
  background: rgba(212,175,55,0.08);
  transform: translateY(-4px);
  box-shadow: 0 10px 30px rgba(212,175,55,0.2);
}

.time-slot.unavailable {
  opacity: 0.35;
  cursor: not-allowed;
}

.time-slot .time { font-family: 'Bebas Neue', cursive; font-size: 1.6rem; color: var(--gold); }
.time-slot .type { font-size: 0.72rem; color: var(--gray); text-transform: uppercase; letter-spacing: 1px; margin-top: 4px; }
.time-slot .spots { font-size: 0.68rem; font-weight: 600; margin-top: 8px; }
.time-slot.available .spots { color: #4CAF50; }
.time-slot.selected .spots { color: var(--gold); }

.selected-check {
  position: absolute;
  top: 8px;
  right: 10px;
  width: 20px;
  height: 20px;
  background: var(--gold);
  color: var(--black);
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 0.65rem;
  opacity: 0;
  transition: opacity 0.3s;
}

.time-slot.selected .selected-check { opacity: 1; }

/* ─── INSCRIPTION FORM ─── */
#inscription { background: var(--dark); }

.form-wrapper {
  max-width: 900px;
  margin: 0 auto;
  background: var(--dark2);
  border: 1px solid var(--dark4);
  border-radius: 32px;
  overflow: hidden;
}

.form-progress {
  display: flex;
  background: var(--dark3);
  border-bottom: 1px solid var(--dark4);
}

.progress-step {
  flex: 1;
  padding: 18px 12px;
  text-align: center;
  font-size: 0.72rem;
  font-weight: 700;
  letter-spacing: 1px;
  text-transform: uppercase;
  color: var(--gray);
  position: relative;
  transition: all 0.3s;
  cursor: pointer;
}

.progress-step.active { color: var(--gold); }
.progress-step.done { color: #4CAF50; }

.progress-step::after {
  content: '';
  position: absolute;
  bottom: 0;
  left: 0;
  right: 0;
  height: 2px;
  background: transparent;
  transition: background 0.3s;
}

.progress-step.active::after { background: var(--gold); }
.progress-step.done::after { background: #4CAF50; }

.step-num {
  display: inline-flex;
  width: 24px;
  height: 24px;
  background: var(--dark4);
  color: var(--gray);
  border-radius: 50%;
  align-items: center;
  justify-content: center;
  font-size: 0.75rem;
  margin-right: 8px;
  font-weight: 800;
  transition: all 0.3s;
}

.progress-step.active .step-num { background: var(--gold); color: var(--black); }
.progress-step.done .step-num { background: #4CAF50; color: white; }

.form-body { padding: 40px; }

.form-step { display: none; }
.form-step.active { display: block; animation: fadeInRight 0.4s ease; }

@keyframes fadeInRight {
  from { opacity: 0; transform: translateX(20px); }
  to { opacity: 1; transform: translateX(0); }
}

.step-title {
  font-family: 'Bebas Neue', cursive;
  font-size: 2.2rem;
  letter-spacing: 1px;
  margin-bottom: 6px;
  color: var(--white);
}

.step-title span { color: var(--gold); }
.step-subtitle { color: var(--gray); font-size: 0.9rem; margin-bottom: 32px; }

.form-grid-2 {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 20px;
  margin-bottom: 20px;
}

.form-grid-3 {
  display: grid;
  grid-template-columns: 1fr 1fr 1fr;
  gap: 20px;
  margin-bottom: 20px;
}

.form-group { display: flex; flex-direction: column; gap: 8px; margin-bottom: 20px; }
.form-group.full { grid-column: 1 / -1; }

.form-group label {
  font-size: 0.72rem;
  font-weight: 700;
  letter-spacing: 1.5px;
  text-transform: uppercase;
  color: var(--gold);
}

.form-group label .req { color: var(--red); margin-left: 3px; }

.form-control {
  background: var(--dark3);
  border: 1px solid var(--dark4);
  color: var(--white);
  padding: 13px 16px;
  border-radius: 12px;
  font-family: 'Barlow', sans-serif;
  font-size: 0.9rem;
  font-weight: 400;
  transition: all 0.3s;
  width: 100%;
}

.form-control:focus {
  outline: none;
  border-color: var(--gold);
  background: rgba(212,175,55,0.05);
}

.form-control::placeholder { color: #555; }

textarea.form-control { resize: vertical; min-height: 90px; }

select.form-control option { background: var(--dark3); }

/* Radio / Checkbox groups */
.radio-group {
  display: flex;
  flex-wrap: wrap;
  gap: 10px;
  margin-top: 4px;
}

.radio-option {
  position: relative;
  cursor: pointer;
}

.radio-option input { position: absolute; opacity: 0; }

.radio-label {
  display: flex;
  align-items: center;
  gap: 8px;
  background: var(--dark3);
  border: 1px solid var(--dark4);
  color: #aaa;
  padding: 10px 18px;
  border-radius: 100px;
  font-size: 0.82rem;
  font-weight: 600;
  cursor: pointer;
  transition: all 0.25s;
  user-select: none;
}

.radio-option input:checked + .radio-label {
  background: rgba(212,175,55,0.12);
  border-color: var(--gold);
  color: var(--gold);
}

/* Health section */
.health-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 16px;
  margin-bottom: 20px;
}

.health-item {
  background: var(--dark3);
  border: 1px solid var(--dark4);
  border-radius: 14px;
  padding: 16px;
}

.health-item label {
  font-size: 0.72rem;
  font-weight: 700;
  letter-spacing: 1px;
  text-transform: uppercase;
  color: #aaa;
  display: block;
  margin-bottom: 10px;
}

.health-item label i { color: var(--gold); margin-right: 6px; }

.health-radios { display: flex; gap: 8px; flex-wrap: wrap; }

.health-radios .radio-option input:checked + .radio-label {
  background: rgba(192,57,43,0.15);
  border-color: var(--red);
  color: #ff7070;
}

.health-radios .radio-option input[value="non"]:checked + .radio-label {
  background: rgba(76,175,80,0.12);
  border-color: #4CAF50;
  color: #81C784;
}

.health-detail {
  margin-top: 10px;
  display: none;
}

.health-detail.visible { display: block; }

/* Upload areas */
.upload-area {
  border: 2px dashed var(--dark4);
  border-radius: 16px;
  padding: 32px;
  text-align: center;
  cursor: pointer;
  transition: all 0.3s;
  position: relative;
}

.upload-area:hover,
.upload-area.dragover {
  border-color: var(--gold);
  background: rgba(212,175,55,0.04);
}

.upload-area input {
  position: absolute;
  inset: 0;
  opacity: 0;
  cursor: pointer;
}

.upload-icon { font-size: 2.4rem; color: var(--gold); margin-bottom: 12px; }
.upload-text { font-size: 0.9rem; color: var(--gray); }
.upload-text strong { color: var(--white); display: block; font-size: 1rem; margin-bottom: 6px; }
.upload-accepted { font-size: 0.72rem; color: #555; margin-top: 8px; letter-spacing: 1px; text-transform: uppercase; }

.upload-preview {
  display: none;
  align-items: center;
  gap: 16px;
  padding: 16px 20px;
  background: rgba(76,175,80,0.08);
  border: 1px solid rgba(76,175,80,0.3);
  border-radius: 12px;
  margin-top: 12px;
}

.upload-preview.visible { display: flex; }
.upload-preview img { width: 60px; height: 60px; object-fit: cover; border-radius: 8px; }
.preview-info { flex: 1; }
.preview-name { font-size: 0.85rem; font-weight: 600; color: var(--white); }
.preview-size { font-size: 0.75rem; color: var(--gray); }
.preview-status { color: #4CAF50; font-size: 0.75rem; font-weight: 600; }

/* Summary box */
.summary-box {
  background: var(--dark3);
  border: 1px solid var(--dark4);
  border-radius: 20px;
  padding: 28px;
  margin-bottom: 24px;
}

.summary-section {
  margin-bottom: 24px;
}

.summary-section:last-child { margin-bottom: 0; }

.summary-section h4 {
  font-size: 0.7rem;
  font-weight: 800;
  letter-spacing: 2px;
  text-transform: uppercase;
  color: var(--gold);
  border-bottom: 1px solid var(--dark4);
  padding-bottom: 10px;
  margin-bottom: 14px;
}

.summary-row {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 6px 0;
  font-size: 0.85rem;
}

.summary-row .key { color: var(--gray); }
.summary-row .val { color: var(--white); font-weight: 600; }
.summary-row .val.highlight { color: var(--gold); font-family: 'Bebas Neue', cursive; font-size: 1.1rem; }

/* Form nav buttons */
.form-nav {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-top: 36px;
  padding-top: 28px;
  border-top: 1px solid var(--dark4);
}

.btn-form-back {
  background: transparent;
  border: 1px solid var(--dark4);
  color: var(--gray);
  padding: 13px 30px;
  border-radius: 100px;
  font-family: 'Barlow', sans-serif;
  font-size: 0.85rem;
  font-weight: 600;
  cursor: pointer;
  transition: all 0.3s;
  display: flex;
  align-items: center;
  gap: 8px;
}

.btn-form-back:hover { border-color: var(--gray); color: var(--white); }

.btn-form-next {
  background: var(--gold);
  border: none;
  color: var(--black);
  padding: 14px 36px;
  border-radius: 100px;
  font-family: 'Barlow', sans-serif;
  font-size: 0.9rem;
  font-weight: 800;
  letter-spacing: 1px;
  text-transform: uppercase;
  cursor: pointer;
  transition: all 0.3s;
  display: flex;
  align-items: center;
  gap: 10px;
}

.btn-form-next:hover {
  background: var(--gold-light);
  transform: translateY(-2px);
  box-shadow: 0 12px 30px rgba(212,175,55,0.3);
}

.btn-form-submit {
  background: var(--red);
  border: none;
  color: var(--white);
  padding: 16px 44px;
  border-radius: 100px;
  font-family: 'Barlow', sans-serif;
  font-size: 1rem;
  font-weight: 800;
  letter-spacing: 1.5px;
  text-transform: uppercase;
  cursor: pointer;
  transition: all 0.3s;
  display: flex;
  align-items: center;
  gap: 10px;
  box-shadow: 0 8px 24px rgba(192,57,43,0.4);
}

.btn-form-submit:hover {
  background: var(--red-dark);
  transform: translateY(-3px);
  box-shadow: 0 16px 40px rgba(192,57,43,0.5);
}

/* ─── CERT ALERT ─── */
.cert-alert {
  background: rgba(212,175,55,0.08);
  border: 1px solid rgba(212,175,55,0.3);
  border-radius: 14px;
  padding: 16px 20px;
  display: flex;
  gap: 14px;
  align-items: flex-start;
  margin-bottom: 24px;
}

.cert-alert i { color: var(--gold); font-size: 1.2rem; flex-shrink: 0; margin-top: 2px; }
.cert-alert p { font-size: 0.82rem; line-height: 1.6; color: #ccc; }
.cert-alert strong { color: var(--gold); }

/* ─── SUCCESS ─── */
.success-screen {
  display: none;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 80px 40px;
  text-align: center;
}

.success-screen.visible { display: flex; }

.success-icon {
  width: 90px;
  height: 90px;
  background: rgba(76,175,80,0.15);
  border: 2px solid rgba(76,175,80,0.4);
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 2.4rem;
  color: #4CAF50;
  margin-bottom: 28px;
  animation: scaleIn 0.6s cubic-bezier(0.34, 1.56, 0.64, 1) both;
}

@keyframes scaleIn {
  from { transform: scale(0); opacity: 0; }
  to { transform: scale(1); opacity: 1; }
}

.success-title {
  font-family: 'Bebas Neue', cursive;
  font-size: 3rem;
  letter-spacing: 2px;
  margin-bottom: 12px;
}

.success-title span { color: var(--gold); }
.success-msg { color: var(--gray); max-width: 500px; line-height: 1.7; font-size: 0.95rem; }

/* ─── STATS STRIP ─── */
.stats-strip {
  background: var(--dark2);
  border-top: 1px solid var(--dark4);
  border-bottom: 1px solid var(--dark4);
  padding: 48px 20px;
}

.stats-grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 20px;
  max-width: 900px;
  margin: 0 auto;
  text-align: center;
}

.stat-num {
  font-family: 'Bebas Neue', cursive;
  font-size: 3.5rem;
  color: var(--gold);
  line-height: 1;
}

.stat-label { font-size: 0.75rem; color: var(--gray); letter-spacing: 1px; text-transform: uppercase; margin-top: 6px; }

/* ─── FOOTER ─── */
footer {
  background: var(--black);
  border-top: 1px solid var(--dark4);
  padding: 40px 20px;
  text-align: center;
  color: var(--gray);
  font-size: 0.82rem;
}

footer .footer-name {
  font-family: 'Bebas Neue', cursive;
  font-size: 1.4rem;
  color: var(--gold);
  letter-spacing: 2px;
  margin-bottom: 8px;
}

/* ─── ANIMATIONS ─── */
@keyframes fadeInDown {
  from { opacity: 0; transform: translateY(-20px); }
  to { opacity: 1; transform: translateY(0); }
}

@keyframes fadeInUp {
  from { opacity: 0; transform: translateY(30px); }
  to { opacity: 1; transform: translateY(0); }
}

.animate-on-scroll {
  opacity: 0;
  transform: translateY(30px);
  transition: opacity 0.6s ease, transform 0.6s ease;
}

.animate-on-scroll.visible {
  opacity: 1;
  transform: translateY(0);
}

/* ─── SELECTED PACK BANNER ─── */
.selected-pack-banner {
  display: none;
  position: fixed;
  bottom: 30px;
  left: 50%;
  transform: translateX(-50%);
  background: var(--gold);
  color: var(--black);
  padding: 14px 30px;
  border-radius: 100px;
  font-weight: 800;
  font-size: 0.9rem;
  letter-spacing: 1px;
  text-transform: uppercase;
  z-index: 1000;
  box-shadow: 0 20px 50px rgba(212,175,55,0.5);
  white-space: nowrap;
  animation: slideUp 0.4s cubic-bezier(0.34, 1.56, 0.64, 1) both;
  cursor: pointer;
}

.selected-pack-banner.visible { display: flex; align-items: center; gap: 12px; }

@keyframes slideUp {
  from { transform: translateX(-50%) translateY(30px); opacity: 0; }
  to { transform: translateX(-50%) translateY(0); opacity: 1; }
}

/* Responsive */
@media (max-width: 768px) {
  .form-grid-2, .form-grid-3 { grid-template-columns: 1fr; }
  .health-grid { grid-template-columns: 1fr; }
  .stats-grid { grid-template-columns: repeat(2, 1fr); }
  .form-body { padding: 24px 20px; }
  .progress-step { font-size: 0; padding: 14px 8px; }
  .progress-step .step-num { margin: 0 auto; display: flex; }
}

@media (max-width: 480px) {
  .packs-grid { grid-template-columns: 1fr; }
  .time-slots { grid-template-columns: repeat(3, 1fr); }
}

/* Floating particles */
.particles {
  position: absolute;
  inset: 0;
  pointer-events: none;
  overflow: hidden;
}

.particle {
  position: absolute;
  width: 4px;
  height: 4px;
  background: var(--gold);
  border-radius: 50%;
  opacity: 0;
  animation: particleFloat var(--dur) var(--delay) ease-in-out infinite;
}

@keyframes particleFloat {
  0% { opacity: 0; transform: translateY(0) scale(0); }
  20% { opacity: 0.6; }
  80% { opacity: 0.3; }
  100% { opacity: 0; transform: translateY(-100px) scale(1.5); }
}

/* Divider */
.divider {
  width: 60px;
  height: 3px;
  background: var(--gold);
  margin: 16px auto 0;
}
</style>
</head>
<body>

<!-- ═══ HERO ═══ -->
<section class="hero" id="accueil">
  <div class="particles" id="particles"></div>
  <div class="hero-bg-text">WUSHU</div>

  <div class="hero-content">
    <div class="hero-badge"><i class="fas fa-fire-alt"></i> &nbsp; Coaching Premium · Fitness & Arts Martiaux</div>

    <h1 class="hero-name">
      MEHDI<br><span>SARKHI</span>
    </h1>

    <p class="hero-tagline">Instructeur Fitness · 5ème Dan Wushu Kung Fu · Coach Sanda</p>

    <div class="hero-credentials">
      <div class="cred-pill"><i class="fas fa-medal"></i> 5ème Dan Wushu Kung Fu</div>
      <div class="cred-pill"><i class="fas fa-trophy"></i> Entraîneur Équipe Régionale Sanda 2008</div>
      <div class="cred-pill"><i class="fas fa-dumbbell"></i> Instructeur Fitness 2ème Degré</div>
      <div class="cred-pill"><i class="fas fa-heartbeat"></i> Remise en forme & Performance</div>
    </div>

    <div class="hero-cta">
      <a href="#inscription" class="btn-primary">
        <i class="fas fa-rocket"></i> Commencer maintenant
      </a>
      <a href="#packs" class="btn-outline-gold">
        <i class="fas fa-list"></i> Voir les packs
      </a>
    </div>

    <!-- GALERIE PHOTOS INSPIRANTES -->
    <div class="hero-gallery">
      <div class="hero-gallery-item">
        <img src="https://images.unsplash.com/photo-1544367567-0f2fcb009e0b?w=400&h=500&fit=crop&crop=center" 
             alt="Wushu Kung Fu" 
             onerror="this.style.display='none'; this.parentElement.innerHTML='<div style=\'width:100%;height:100%;display:flex;align-items:center;justify-content:center;color:var(--gold);font-size:2rem;\'><i class=\'fas fa-fist-raised\'></i></div>'">
        <div class="gallery-label">Wushu Kung Fu</div>
      </div>
      <div class="hero-gallery-item">
        <img src="https://images.unsplash.com/photo-1571019614242-c5c5dee9f50b?w=400&h=500&fit=crop&crop=center" 
             alt="Fitness" 
             onerror="this.style.display='none'; this.parentElement.innerHTML='<div style=\'width:100%;height:100%;display:flex;align-items:center;justify-content:center;color:var(--gold);font-size:2rem;\'><i class=\'fas fa-dumbbell\'></i></div>'">
        <div class="gallery-label">Fitness</div>
      </div>
      <div class="hero-gallery-item">
        <img src="https://images.unsplash.com/photo-1599058945522-28d584b6f0ff?w=400&h=500&fit=crop&crop=center" 
             alt="Sanda" 
             onerror="this.style.display='none'; this.parentElement.innerHTML='<div style=\'width:100%;height:100%;display:flex;align-items:center;justify-content:center;color:var(--gold);font-size:2rem;\'><i class=\'fas fa-boxing-glove\'></i></div>'">
        <div class="gallery-label">Sanda Combat</div>
      </div>
      <div class="hero-gallery-item">
        <img src="https://images.unsplash.com/photo-1518611012118-a3e5e53a1b0f?w=400&h=500&fit=crop&crop=center" 
             alt="Cardio" 
             onerror="this.style.display='none'; this.parentElement.innerHTML='<div style=\'width:100%;height:100%;display:flex;align-items:center;justify-content:center;color:var(--gold);font-size:2rem;\'><i class=\'fas fa-heartbeat\'></i></div>'">
        <div class="gallery-label">Cardio Training</div>
      </div>
    </div>
  </div>

  <div class="scroll-indicator">
    <span>Découvrir</span>
    <i class="fas fa-chevron-down"></i>
  </div>
</section>

<!-- ═══ STATS ═══ -->
<div class="stats-strip">
  <div class="stats-grid">
    <div class="animate-on-scroll">
      <div class="stat-num">5</div>
      <div class="stat-label">Disciplines enseignées</div>
    </div>
    <div class="animate-on-scroll" style="transition-delay:0.1s">
      <div class="stat-num">15+</div>
      <div class="stat-label">Années d'expérience</div>
    </div>
    <div class="animate-on-scroll" style="transition-delay:0.2s">
      <div class="stat-num">200+</div>
      <div class="stat-label">Clients accompagnés</div>
    </div>
    <div class="animate-on-scroll" style="transition-delay:0.3s">
      <div class="stat-num">100%</div>
      <div class="stat-label">Suivi personnalisé</div>
    </div>
  </div>
</div>

<!-- ═══ PACKS ═══ -->
<section id="packs">
  <div class="section-header animate-on-scroll">
    <div class="section-label"><i class="fas fa-tag"></i> Nos formules</div>
    <h2 class="section-title">CHOISISSEZ <span>VOTRE PACK</span></h2>
    <div class="divider"></div>
    <p class="section-sub">Des formules adaptées à chaque objectif et chaque budget</p>
  </div>

  <div class="packs-grid">
    <div class="pack-card animate-on-scroll" data-pack="Découverte" data-sessions="1" data-price="50" data-validity="30" onclick="selectPack(this)">
      <div class="pack-icon"><i class="fas fa-play"></i></div>
      <div class="pack-name">Découverte</div>
      <div class="pack-sessions">1 séance individuelle</div>
      <div class="pack-price"><span>€</span>50</div>
      <div class="pack-validity">Validité 30 jours</div>
      <ul class="pack-features">
        <li><i class="fas fa-check"></i> 1 séance d'1h</li>
        <li><i class="fas fa-check"></i> Bilan initial complet</li>
        <li><i class="fas fa-check"></i> Programme personnalisé</li>
        <li><i class="fas fa-check"></i> Suivi coach en direct</li>
      </ul>
      <button class="pack-select-btn">Choisir ce pack</button>
    </div>

    <div class="pack-card animate-on-scroll" data-pack="Forme +" data-sessions="5" data-price="230" data-validity="60" onclick="selectPack(this)" style="transition-delay:0.1s">
      <div class="pack-icon"><i class="fas fa-fire"></i></div>
      <div class="pack-name">Forme +</div>
      <div class="pack-sessions">5 séances individuelles</div>
      <div class="pack-price"><span>€</span>230</div>
      <div class="pack-validity">Validité 60 jours</div>
      <ul class="pack-features">
        <li><i class="fas fa-check"></i> 5 séances d'1h chacune</li>
        <li><i class="fas fa-check"></i> Bilan + suivi mensuel</li>
        <li><i class="fas fa-check"></i> Programme adaptatif</li>
        <li><i class="fas fa-check"></i> Nutrition de base</li>
      </ul>
      <button class="pack-select-btn">Choisir ce pack</button>
    </div>

    <div class="pack-card featured animate-on-scroll" data-pack="Performance" data-sessions="10" data-price="420" data-validity="90" onclick="selectPack(this)" style="transition-delay:0.2s">
      <div class="pack-badge-featured">⭐ Populaire</div>
      <div class="pack-icon"><i class="fas fa-bolt"></i></div>
      <div class="pack-name">Performance</div>
      <div class="pack-sessions">10 séances individuelles</div>
      <div class="pack-price"><span>€</span>420</div>
      <div class="pack-validity">Validité 90 jours</div>
      <ul class="pack-features">
        <li><i class="fas fa-check"></i> 10 séances d'1h</li>
        <li><i class="fas fa-check"></i> Suivi biométrique complet</li>
        <li><i class="fas fa-check"></i> Plan nutrition personnalisé</li>
        <li><i class="fas fa-check"></i> Accès programme en ligne</li>
      </ul>
      <button class="pack-select-btn">Choisir ce pack</button>
    </div>

    <div class="pack-card animate-on-scroll" data-pack="Illimité 1 mois" data-sessions="0" data-price="300" data-validity="30" onclick="selectPack(this)" style="transition-delay:0.3s">
      <div class="pack-icon"><i class="fas fa-infinity"></i></div>
      <div class="pack-name">Illimité</div>
      <div class="pack-sessions">Séances illimitées / mois</div>
      <div class="pack-price"><span>€</span>300</div>
      <div class="pack-validity">Validité 30 jours</div>
      <ul class="pack-features">
        <li><i class="fas fa-check"></i> Accès illimité 1 mois</li>
        <li><i class="fas fa-check"></i> Tous types de séances</li>
        <li><i class="fas fa-check"></i> Suivi complet quotidien</li>
        <li><i class="fas fa-check"></i> Priorité horaires</li>
      </ul>
      <button class="pack-select-btn">Choisir ce pack</button>
    </div>
  </div>
</section>

<!-- ═══ HORAIRES ═══ -->
<section id="horaires">
  <div class="section-header animate-on-scroll">
    <div class="section-label"><i class="fas fa-clock"></i> Planning</div>
    <h2 class="section-title">CHOISISSEZ <span>VOS CRÉNEAUX</span></h2>
    <div class="divider"></div>
    <p class="section-sub">Sélectionnez votre jour et votre horaire préféré</p>
  </div>

  <div class="horaires-container animate-on-scroll">
    <div class="days-tabs" id="daysTabs">
      <button class="day-tab active" data-day="lundi" onclick="selectDay(this)">Lundi</button>
      <button class="day-tab" data-day="mardi" onclick="selectDay(this)">Mardi</button>
      <button class="day-tab" data-day="mercredi" onclick="selectDay(this)">Mercredi</button>
      <button class="day-tab" data-day="jeudi" onclick="selectDay(this)">Jeudi</button>
      <button class="day-tab" data-day="vendredi" onclick="selectDay(this)">Vendredi</button>
      <button class="day-tab" data-day="samedi" onclick="selectDay(this)">Samedi</button>
    </div>

    <div class="time-slots" id="timeSlots"></div>
  </div>
</section>

<!-- ═══ INSCRIPTION ═══ -->
<section id="inscription">
  <div class="section-header animate-on-scroll">
    <div class="section-label"><i class="fas fa-user-plus"></i> Inscription</div>
    <h2 class="section-title">CRÉEZ <span>VOTRE PROFIL</span></h2>
    <div class="divider"></div>
    <p class="section-sub">Remplissez votre fiche de première séance complète</p>
  </div>

  <div class="form-wrapper animate-on-scroll">

    <!-- Progress -->
    <div class="form-progress">
      <div class="progress-step active" data-step="1" onclick="goToStep(1)">
        <span class="step-num">1</span><span class="step-label">Identité</span>
      </div>
      <div class="progress-step" data-step="2" onclick="goToStep(2)">
        <span class="step-num">2</span><span class="step-label">Santé</span>
      </div>
      <div class="progress-step" data-step="3" onclick="goToStep(3)">
        <span class="step-num">3</span><span class="step-label">Sport</span>
      </div>
      <div class="progress-step" data-step="4" onclick="goToStep(4)">
        <span class="step-num">4</span><span class="step-label">Biométrie</span>
      </div>
      <div class="progress-step" data-step="5" onclick="goToStep(5)">
        <span class="step-num">5</span><span class="step-label">Documents</span>
      </div>
      <div class="progress-step" data-step="6" onclick="goToStep(6)">
        <span class="step-num">6</span><span class="step-label">Récap</span>
      </div>
    </div>

    <div class="form-body">

      <!-- STEP 1 : IDENTITÉ -->
      <div class="form-step active" id="step1">
        <h3 class="step-title">Vos <span>Coordonnées</span></h3>
        <p class="step-subtitle">Informations personnelles (conformes à la fiche 1ère séance)</p>

        <div class="form-grid-2">
          <div class="form-group">
            <label>Nom <span class="req">*</span></label>
            <input class="form-control" type="text" id="fNom" placeholder="DUPONT" required>
          </div>
          <div class="form-group">
            <label>Prénom <span class="req">*</span></label>
            <input class="form-control" type="text" id="fPrenom" placeholder="Marie" required>
          </div>
          <div class="form-group">
            <label>Date de naissance <span class="req">*</span></label>
            <input class="form-control" type="date" id="fDdn" required>
          </div>
          <div class="form-group">
            <label>Lieu de naissance</label>
            <input class="form-control" type="text" id="fLdn" placeholder="Paris">
          </div>
          <div class="form-group">
            <label>Téléphone mobile <span class="req">*</span></label>
            <input class="form-control" type="tel" id="fTel" placeholder="+33 6 00 00 00 00" required>
          </div>
          <div class="form-group">
            <label>E-mail <span class="req">*</span></label>
            <input class="form-control" type="email" id="fEmail" placeholder="votre@email.com" required>
          </div>
          <div class="form-group full">
            <label>Adresse complète</label>
            <input class="form-control" type="text" id="fAdresse" placeholder="12 Rue des Sports, Paris 75001">
          </div>
          <div class="form-group full">
            <label>Vos motivations <span class="req">*</span></label>
            <textarea class="form-control" id="fMotivations" placeholder="Pourquoi souhaitez-vous vous entraîner avec Coach Mehdi ? (perte de poids, performance, bien-être, compétition...)" required></textarea>
          </div>
          <div class="form-group full">
            <label>Vos disponibilités</label>
            <div class="radio-group">
              <label class="radio-option"><input type="checkbox" name="dispo" value="Matin (7h-12h)"><span class="radio-label"><i class="fas fa-sun"></i> Matin</span></label>
              <label class="radio-option"><input type="checkbox" name="dispo" value="Midi (12h-14h)"><span class="radio-label"><i class="fas fa-cloud-sun"></i> Midi</span></label>
              <label class="radio-option"><input type="checkbox" name="dispo" value="Après-midi (14h-18h)"><span class="radio-label"><i class="fas fa-cloud"></i> Après-midi</span></label>
              <label class="radio-option"><input type="checkbox" name="dispo" value="Soir (18h-21h)"><span class="radio-label"><i class="fas fa-moon"></i> Soir</span></label>
              <label class="radio-option"><input type="checkbox" name="dispo" value="Week-end"><span class="radio-label"><i class="fas fa-calendar-week"></i> Week-end</span></label>
            </div>
          </div>
        </div>

        <div class="form-nav">
          <div></div>
          <button class="btn-form-next" onclick="nextStep(1)">Suivant – Santé <i class="fas fa-arrow-right"></i></button>
        </div>
      </div>

      <!-- STEP 2 : SANTÉ -->
      <div class="form-step" id="step2">
        <h3 class="step-title">Informations de <span>Santé</span></h3>
        <p class="step-subtitle">Ces informations permettent au coach d'adapter votre programme en toute sécurité</p>

        <div class="cert-alert">
          <i class="fas fa-certificate"></i>
          <p><strong>Certificat médical obligatoire :</strong> un certificat médical de non-contre-indication à la pratique sportive doit être remis dans un délai d'<strong>1 mois</strong> après votre première séance.</p>
        </div>

        <div class="health-grid">
          <div class="health-item">
            <label><i class="fas fa-brain"></i> Stress</label>
            <div class="health-radios radio-group">
              <label class="radio-option"><input type="radio" name="hStress" value="non"><span class="radio-label">Non</span></label>
              <label class="radio-option"><input type="radio" name="hStress" value="oui"><span class="radio-label">Oui</span></label>
            </div>
            <div class="health-detail" id="detailStress">
              <input class="form-control" style="margin-top:8px;" type="text" id="fStressDetail" placeholder="Cause / précisions">
            </div>
          </div>
          <div class="health-item">
            <label><i class="fas fa-bed"></i> Insomnies</label>
            <div class="health-radios radio-group">
              <label class="radio-option"><input type="radio" name="hInsom" value="non"><span class="radio-label">Non</span></label>
              <label class="radio-option"><input type="radio" name="hInsom" value="oui"><span class="radio-label">Oui</span></label>
            </div>
          </div>
          <div class="health-item">
            <label><i class="fas fa-head-side-virus"></i> Migraines / Vertiges</label>
            <div class="health-radios radio-group">
              <label class="radio-option"><input type="radio" name="hMig" value="non"><span class="radio-label">Non</span></label>
              <label class="radio-option"><input type="radio" name="hMig" value="oui"><span class="radio-label">Oui</span></label>
            </div>
            <div class="health-detail" id="detailMig">
              <input class="form-control" style="margin-top:8px;" type="text" id="fMigDetail" placeholder="Étourdissements, perte d'équilibre...">
            </div>
          </div>
          <div class="health-item">
            <label><i class="fas fa-heart"></i> Problèmes cardiaques</label>
            <div class="health-radios radio-group">
              <label class="radio-option"><input type="radio" name="hCar" value="non"><span class="radio-label">Non</span></label>
              <label class="radio-option"><input type="radio" name="hCar" value="oui"><span class="radio-label">Oui</span></label>
            </div>
            <div class="health-detail" id="detailCar">
              <input class="form-control" style="margin-top:8px;" type="text" id="fCarDetail" placeholder="Angine, arythmie, antécédents familiaux...">
            </div>
          </div>
          <div class="health-item">
            <label><i class="fas fa-lungs"></i> Problèmes respiratoires</label>
            <div class="health-radios radio-group">
              <label class="radio-option"><input type="radio" name="hResp" value="non"><span class="radio-label">Non</span></label>
              <label class="radio-option"><input type="radio" name="hResp" value="oui"><span class="radio-label">Oui</span></label>
            </div>
            <div class="health-detail" id="detailResp">
              <input class="form-control" style="margin-top:8px;" type="text" id="fRespDetail" placeholder="Asthme, apnée...">
            </div>
          </div>
          <div class="health-item">
            <label><i class="fas fa-tint"></i> Problèmes circulatoires</label>
            <div class="health-radios radio-group">
              <label class="radio-option"><input type="radio" name="hCirc" value="non"><span class="radio-label">Non</span></label>
              <label class="radio-option"><input type="radio" name="hCirc" value="oui"><span class="radio-label">Oui</span></label>
            </div>
            <div class="health-detail" id="detailCirc">
              <input class="form-control" style="margin-top:8px;" type="text" id="fCircDetail" placeholder="Phlébite, hyper/hypotension, varices...">
            </div>
          </div>
          <div class="health-item">
            <label><i class="fas fa-bone"></i> Problèmes de dos</label>
            <div class="health-radios radio-group">
              <label class="radio-option"><input type="radio" name="hDos" value="non"><span class="radio-label">Non</span></label>
              <label class="radio-option"><input type="radio" name="hDos" value="oui"><span class="radio-label">Oui</span></label>
            </div>
            <div class="health-detail" id="detailDos">
              <input class="form-control" style="margin-top:8px;" type="text" id="fDosDetail" placeholder="Cyphose, scoliose, hernie discale...">
            </div>
          </div>
          <div class="health-item">
            <label><i class="fas fa-running"></i> Problèmes ostéo-articulaires</label>
            <div class="health-radios radio-group">
              <label class="radio-option"><input type="radio" name="hOsteo" value="non"><span class="radio-label">Non</span></label>
              <label class="radio-option"><input type="radio" name="hOsteo" value="oui"><span class="radio-label">Oui</span></label>
            </div>
            <div class="health-detail" id="detailOsteo">
              <input class="form-control" style="margin-top:8px;" type="text" id="fOsteoDetail" placeholder="Arthrite, arthrose, fractures...">
            </div>
          </div>
          <div class="health-item">
            <label><i class="fas fa-band-aid"></i> Problèmes musculo-tendineux</label>
            <div class="health-radios radio-group">
              <label class="radio-option"><input type="radio" name="hMusc" value="non"><span class="radio-label">Non</span></label>
              <label class="radio-option"><input type="radio" name="hMusc" value="oui"><span class="radio-label">Oui</span></label>
            </div>
            <div class="health-detail" id="detailMusc">
              <input class="form-control" style="margin-top:8px;" type="text" id="fMuscDetail" placeholder="Déchirures, tendinites, crampes...">
            </div>
          </div>
          <div class="health-item">
            <label><i class="fas fa-allergies"></i> Allergies / Intolérances alimentaires</label>
            <div class="health-radios radio-group">
              <label class="radio-option"><input type="radio" name="hAll" value="non"><span class="radio-label">Non</span></label>
              <label class="radio-option"><input type="radio" name="hAll" value="oui"><span class="radio-label">Oui</span></label>
            </div>
            <div class="health-detail" id="detailAll">
              <input class="form-control" style="margin-top:8px;" type="text" id="fAllDetail" placeholder="Gluten, lactose, acariens...">
            </div>
          </div>
          <div class="health-item">
            <label><i class="fas fa-baby"></i> Grossesse / Mouvements interdits</label>
            <div class="health-radios radio-group">
              <label class="radio-option"><input type="radio" name="hGross" value="non"><span class="radio-label">Non</span></label>
              <label class="radio-option"><input type="radio" name="hGross" value="oui"><span class="radio-label">Oui</span></label>
            </div>
            <div class="health-detail" id="detailGross">
              <input class="form-control" style="margin-top:8px;" type="text" id="fGrossDetail" placeholder="Mouvements à éviter...">
            </div>
          </div>
          <div class="health-item">
            <label><i class="fas fa-smoking"></i> Tabagisme</label>
            <div class="health-radios radio-group">
              <label class="radio-option"><input type="radio" name="hFum" value="non"><span class="radio-label">Non</span></label>
              <label class="radio-option"><input type="radio" name="hFum" value="oui"><span class="radio-label">Oui</span></label>
            </div>
          </div>
        </div>

        <div class="form-group">
          <label>Traitement médical en cours</label>
          <input class="form-control" type="text" id="fTraitement" placeholder="Médicaments, posologie... (ou 'aucun')">
        </div>
        <div class="form-group">
          <label>Autres problèmes de santé</label>
          <input class="form-control" type="text" id="fAutresSante" placeholder="Spasmophilie, tétanie, hernies, problèmes hépatiques ou rénaux...">
        </div>

        <div class="form-nav">
          <button class="btn-form-back" onclick="prevStep(2)"><i class="fas fa-arrow-left"></i> Retour</button>
          <button class="btn-form-next" onclick="nextStep(2)">Suivant – Sport <i class="fas fa-arrow-right"></i></button>
        </div>
      </div>

      <!-- STEP 3 : SPORT & QUOTIDIEN -->
      <div class="form-step" id="step3">
        <h3 class="step-title">Informations <span>Sportives</span></h3>
        <p class="step-subtitle">Activité physique actuelle et quotidienne</p>

        <div class="form-grid-2">
          <div class="form-group">
            <label>Pratiquez-vous une activité sportive ?</label>
            <div class="radio-group">
              <label class="radio-option"><input type="radio" name="fSport" value="Oui"><span class="radio-label">Oui</span></label>
              <label class="radio-option"><input type="radio" name="fSport" value="Non"><span class="radio-label">Non</span></label>
            </div>
          </div>
          <div class="form-group">
            <label>Nombre de séances / semaine</label>
            <select class="form-control" id="fNbSeances">
              <option value="">-- Choisir --</option>
              <option>1x</option><option>2x</option><option>3x</option>
              <option>4x</option><option>5x</option><option>6x ou +</option>
            </select>
          </div>
          <div class="form-group">
            <label>Durée des séances</label>
            <select class="form-control" id="fDureeSeances">
              <option value="">-- Choisir --</option>
              <option>30 min</option><option>45 min</option><option>1h</option>
              <option>1h30</option><option>2h ou +</option>
            </select>
          </div>
          <div class="form-group">
            <label>Passé sportif</label>
            <input class="form-control" type="text" id="fPasse" placeholder="Ex: Football 5 ans, Natation, Arts martiaux...">
          </div>
        </div>

        <h4 style="color:var(--gold); font-size:0.75rem; letter-spacing:2px; text-transform:uppercase; margin: 28px 0 16px; font-weight:800;">— Activité Quotidienne</h4>

        <div class="form-grid-2">
          <div class="form-group">
            <label>Profession</label>
            <input class="form-control" type="text" id="fProfession" placeholder="Enseignant(e), développeur(se)...">
          </div>
          <div class="form-group">
            <label>Heures de travail / jour</label>
            <select class="form-control" id="fHeuresTravail">
              <option value="">-- Choisir --</option>
              <option>Moins de 4h</option><option>4h à 6h</option>
              <option>6h à 8h</option><option>8h à 10h</option><option>Plus de 10h</option>
            </select>
          </div>
          <div class="form-group">
            <label>Vous êtes le plus souvent</label>
            <div class="radio-group">
              <label class="radio-option"><input type="radio" name="fPosQuot" value="Assis"><span class="radio-label"><i class="fas fa-chair"></i> Assis</span></label>
              <label class="radio-option"><input type="radio" name="fPosQuot" value="Debout"><span class="radio-label"><i class="fas fa-male"></i> Debout</span></label>
              <label class="radio-option"><input type="radio" name="fPosQuot" value="En mouvement"><span class="radio-label"><i class="fas fa-walking"></i> En mouvement</span></label>
            </div>
          </div>
          <div class="form-group">
            <label>Vie générale</label>
            <div class="radio-group">
              <label class="radio-option"><input type="radio" name="fVieActive" value="Peu active"><span class="radio-label">Peu active</span></label>
              <label class="radio-option"><input type="radio" name="fVieActive" value="Active"><span class="radio-label">Active</span></label>
              <label class="radio-option"><input type="radio" name="fVieActive" value="Très active"><span class="radio-label">Très active</span></label>
            </div>
          </div>
        </div>

        <h4 style="color:var(--gold); font-size:0.75rem; letter-spacing:2px; text-transform:uppercase; margin: 28px 0 16px; font-weight:800;">— Informations Nutritionnelles</h4>

        <div class="form-grid-3">
          <div class="form-group">
            <label>Alimentation équilibrée ?</label>
            <div class="radio-group">
              <label class="radio-option"><input type="radio" name="fEquil" value="Oui"><span class="radio-label">Oui</span></label>
              <label class="radio-option"><input type="radio" name="fEquil" value="Non"><span class="radio-label">Non</span></label>
            </div>
          </div>
          <div class="form-group">
            <label>Vous mangez</label>
            <div class="radio-group">
              <label class="radio-option"><input type="radio" name="fMange" value="Peu"><span class="radio-label">Peu</span></label>
              <label class="radio-option"><input type="radio" name="fMange" value="Suffisamment"><span class="radio-label">Suffisamment</span></label>
              <label class="radio-option"><input type="radio" name="fMange" value="Trop"><span class="radio-label">Trop</span></label>
            </div>
          </div>
          <div class="form-group">
            <label>Eau par jour</label>
            <div class="radio-group">
              <label class="radio-option"><input type="radio" name="fEau" value="Moins d'1L"><span class="radio-label">&lt; 1L</span></label>
              <label class="radio-option"><input type="radio" name="fEau" value="1-2L"><span class="radio-label">1–2L</span></label>
              <label class="radio-option"><input type="radio" name="fEau" value="Plus de 2L"><span class="radio-label">&gt; 2L</span></label>
            </div>
          </div>
          <div class="form-group">
            <label>Café / jour</label>
            <input class="form-control" type="number" id="fCafe" placeholder="0" min="0">
          </div>
          <div class="form-group">
            <label>Thé / jour</label>
            <input class="form-control" type="number" id="fThe" placeholder="0" min="0">
          </div>
          <div class="form-group">
            <label>Boissons sucrées / jour</label>
            <input class="form-control" type="number" id="fSucre" placeholder="0" min="0">
          </div>
        </div>

        <div class="form-nav">
          <button class="btn-form-back" onclick="prevStep(3)"><i class="fas fa-arrow-left"></i> Retour</button>
          <button class="btn-form-next" onclick="nextStep(3)">Suivant – Biométrie <i class="fas fa-arrow-right"></i></button>
        </div>
      </div>

      <!-- STEP 4 : BIOMÉTRIE -->
      <div class="form-step" id="step4">
        <h3 class="step-title">Biométrie & <span>Tests Physiques</span></h3>
        <p class="step-subtitle">Ces données seront remplies / vérifiées lors de votre 1ère séance avec Coach Mehdi</p>

        <div class="form-grid-3">
          <div class="form-group">
            <label>Poids actuel (kg)</label>
            <input class="form-control" type="number" id="fPoidsActuel" placeholder="70" step="0.1">
          </div>
          <div class="form-group">
            <label>Poids le plus haut (kg)</label>
            <input class="form-control" type="number" id="fPoidsHaut" placeholder="80" step="0.1">
          </div>
          <div class="form-group">
            <label>Poids désiré (kg)</label>
            <input class="form-control" type="number" id="fPoidsDesire" placeholder="65" step="0.1">
          </div>
          <div class="form-group">
            <label>Taille (cm)</label>
            <input class="form-control" type="number" id="fTaille" placeholder="170">
          </div>
          <div class="form-group">
            <label>IMC (calculé auto)</label>
            <input class="form-control" type="text" id="fIMC" placeholder="Calculé..." readonly style="opacity:0.6">
          </div>
          <div class="form-group">
            <label>% Masse grasse (estimé)</label>
            <input class="form-control" type="number" id="fMasseGrasse" placeholder="25" step="0.1">
          </div>
        </div>

        <h4 style="color:var(--gold); font-size:0.75rem; letter-spacing:2px; text-transform:uppercase; margin: 28px 0 16px; font-weight:800;">— Tests d'Aptitudes Physiques (facultatif)</h4>

        <div class="form-grid-3">
          <div class="form-group">
            <label>Test Ruffier – FC0 (repos)</label>
            <input class="form-control" type="number" id="fFC0" placeholder="bpm">
          </div>
          <div class="form-group">
            <label>Test Ruffier – FC1 (après effort)</label>
            <input class="form-control" type="number" id="fFC1" placeholder="bpm">
          </div>
          <div class="form-group">
            <label>Test Ruffier – FC2 (1min après)</label>
            <input class="form-control" type="number" id="fFC2" placeholder="bpm">
          </div>
          <div class="form-group">
            <label>Indice Ruffier (calculé)</label>
            <input class="form-control" type="text" id="fRuffier" placeholder="Calculé..." readonly style="opacity:0.6">
          </div>
          <div class="form-group">
            <label>Pompes (max)</label>
            <input class="form-control" type="number" id="fPompes" placeholder="15">
          </div>
          <div class="form-group">
            <label>Chaise (secondes)</label>
            <input class="form-control" type="number" id="fChaise" placeholder="60">
          </div>
        </div>

        <div class="form-grid-2">
          <div class="form-group">
            <label>Souplesse – Flexion du tronc</label>
            <div class="radio-group">
              <label class="radio-option"><input type="radio" name="fSoup" value="Mauvaise"><span class="radio-label">Mauvaise</span></label>
              <label class="radio-option"><input type="radio" name="fSoup" value="Moyenne"><span class="radio-label">Moyenne</span></label>
              <label class="radio-option"><input type="radio" name="fSoup" value="Bonne"><span class="radio-label">Bonne</span></label>
              <label class="radio-option"><input type="radio" name="fSoup" value="Excellente"><span class="radio-label">Excellente</span></label>
            </div>
          </div>
          <div class="form-group">
            <label>Souplesse – Scapulaire</label>
            <div class="radio-group">
              <label class="radio-option"><input type="radio" name="fScap" value="Mauvaise"><span class="radio-label">Mauvaise</span></label>
              <label class="radio-option"><input type="radio" name="fScap" value="Moyenne"><span class="radio-label">Moyenne</span></label>
              <label class="radio-option"><input type="radio" name="fScap" value="Bonne"><span class="radio-label">Bonne</span></label>
            </div>
          </div>
        </div>

        <div class="form-nav">
          <button class="btn-form-back" onclick="prevStep(4)"><i class="fas fa-arrow-left"></i> Retour</button>
          <button class="btn-form-next" onclick="nextStep(4)">Suivant – Documents <i class="fas fa-arrow-right"></i></button>
        </div>
      </div>

      <!-- STEP 5 : DOCUMENTS -->
      <div class="form-step" id="step5">
        <h3 class="step-title">Vos <span>Documents</span></h3>
        <p class="step-subtitle">Photo d'identité et certificat médical</p>

        <div class="cert-alert">
          <i class="fas fa-info-circle"></i>
          <p>La photo d'identité est obligatoire pour créer votre profil. Le certificat médical de non-contre-indication à la pratique sportive est <strong>obligatoire dans un délai d'1 mois</strong>. Vous pouvez l'uploader maintenant ou l'apporter lors de votre première séance.</p>
        </div>

        <div class="form-group">
          <label>Photo d'identité <span class="req">*</span></label>
          <div class="upload-area" id="photoArea">
            <input type="file" id="fPhoto" accept="image/jpeg,image/png,image/webp" onchange="handleFileUpload('photo', this)">
            <div class="upload-icon"><i class="fas fa-user-circle"></i></div>
            <div class="upload-text">
              <strong>Glissez votre photo ici</strong>
              Ou cliquez pour sélectionner
            </div>
            <div class="upload-accepted">JPEG · PNG · WEBP · Max 5MB</div>
          </div>
          <div class="upload-preview" id="photoPreview">
            <img id="photoThumb" src="" alt="">
            <div class="preview-info">
              <div class="preview-name" id="photoName">photo.jpg</div>
              <div class="preview-size" id="photoSize">2.4 MB</div>
              <div class="preview-status"><i class="fas fa-check-circle"></i> Photo téléchargée</div>
            </div>
          </div>
        </div>

        <div class="form-group">
          <label>Certificat médical <span style="color:var(--gray); font-weight:400;">(facultatif maintenant)</span></label>
          <div class="upload-area" id="certifArea">
            <input type="file" id="fCertif" accept="image/jpeg,image/png,application/pdf" onchange="handleFileUpload('certif', this)">
            <div class="upload-icon"><i class="fas fa-file-medical"></i></div>
            <div class="upload-text">
              <strong>Glissez votre certificat médical ici</strong>
              Ou cliquez pour sélectionner
            </div>
            <div class="upload-accepted">JPEG · PNG · PDF · Max 10MB</div>
          </div>
          <div class="upload-preview" id="certifPreview">
            <div style="font-size:2rem; color:#4CAF50;"><i class="fas fa-file-medical-alt"></i></div>
            <div class="preview-info">
              <div class="preview-name" id="certifName">certificat.pdf</div>
              <div class="preview-size" id="certifSize">1.2 MB</div>
              <div class="preview-status"><i class="fas fa-check-circle"></i> Certificat téléchargé</div>
            </div>
          </div>
        </div>

        <div class="form-nav">
          <button class="btn-form-back" onclick="prevStep(5)"><i class="fas fa-arrow-left"></i> Retour</button>
          <button class="btn-form-next" onclick="nextStep(5)">Suivant – Récapitulatif <i class="fas fa-arrow-right"></i></button>
        </div>
      </div>

      <!-- STEP 6 : RÉCAP -->
      <div class="form-step" id="step6">
        <h3 class="step-title">Récapitulatif & <span>Confirmation</span></h3>
        <p class="step-subtitle">Vérifiez vos informations avant d'envoyer votre inscription</p>

        <div class="summary-box" id="summaryBox"></div>

        <div style="margin-bottom:20px; display:flex; align-items:flex-start; gap:12px; background:var(--dark3); border:1px solid var(--dark4); border-radius:14px; padding:20px;">
          <input type="checkbox" id="fCGV" style="width:20px; height:20px; margin-top:2px; accent-color:var(--gold); cursor:pointer; flex-shrink:0;">
          <label for="fCGV" style="font-size:0.85rem; color:#aaa; cursor:pointer; line-height:1.5;">
            J'accepte les conditions générales et confirme l'exactitude des informations fournies. Je comprends que le certificat médical doit être remis dans un délai d'<strong style="color:var(--gold)">1 mois</strong>.
          </label>
        </div>

        <div class="form-nav">
          <button class="btn-form-back" onclick="prevStep(6)"><i class="fas fa-arrow-left"></i> Retour</button>
          <button class="btn-form-submit" onclick="submitForm()">
            <i class="fas fa-paper-plane"></i> Envoyer mon inscription
          </button>
        </div>
      </div>

    </div><!-- /form-body -->

    <!-- SUCCESS -->
    <div class="success-screen" id="successScreen">
      <div class="success-icon"><i class="fas fa-check"></i></div>
      <h3 class="success-title">INSCRIPTION <span>ENVOYÉE !</span></h3>
      <p class="success-msg">
        Félicitations ! Votre fiche de première séance a été transmise à Coach Mehdi Sarkhi. Vous recevrez une confirmation de votre rendez-vous dans les 24h.
        <br><br>
        <strong style="color:var(--gold)">N'oubliez pas votre certificat médical !</strong> Vous avez 1 mois pour le remettre.
      </p>
    </div>

  </div>
</section>

<!-- ─── FOOTER ─── -->
<footer>
  <div class="footer-name">MEHDI SARKHI</div>
  <p>Instructeur Fitness & Remise en Forme · 2ème Degré · 5ème Dan Wushu Kung Fu</p>
  <p style="margin-top:8px; color:#444;">Entraîneur Équipe Régionale Sanda 2008</p>
  <button onclick="openCoachLogin()" style="margin-top:28px;background:transparent;border:1px solid #2a2a2a;color:#333;padding:8px 20px;border-radius:100px;font-family:'Barlow',sans-serif;font-size:0.72rem;letter-spacing:2px;text-transform:uppercase;cursor:pointer;transition:all 0.3s;" onmouseover="this.style.borderColor='#D4AF37';this.style.color='#D4AF37';" onmouseout="this.style.borderColor='#2a2a2a';this.style.color='#333';"><i class="fas fa-lock" style="margin-right:6px;"></i>Accès Coach</button>
</footer>

<!-- ═══════════════════════════════════════════════════
     DASHBOARD COACH – OVERLAY
═══════════════════════════════════════════════════ -->
<div id="coachOverlay" style="display:none;position:fixed;inset:0;background:rgba(0,0,0,0.96);z-index:9999;overflow-y:auto;font-family:'Barlow',sans-serif;">

  <!-- LOGIN SCREEN -->
  <div id="coachLogin" style="display:flex;align-items:center;justify-content:center;min-height:100vh;padding:20px;">
    <div style="background:#111;border:1px solid #222;border-radius:24px;padding:48px 40px;width:100%;max-width:400px;text-align:center;">
      <div style="width:64px;height:64px;background:rgba(212,175,55,0.1);border:1px solid rgba(212,175,55,0.3);border-radius:50%;display:flex;align-items:center;justify-content:center;margin:0 auto 24px;font-size:1.6rem;color:#D4AF37;"><i class="fas fa-shield-alt"></i></div>
      <div style="font-family:'Bebas Neue',cursive;font-size:2rem;letter-spacing:2px;color:#fff;margin-bottom:6px;">ESPACE COACH</div>
      <p style="color:#555;font-size:0.85rem;margin-bottom:32px;">Accès réservé – Mehdi Sarkhi</p>
      <input id="coachPwdInput" type="password" placeholder="Mot de passe" onkeydown="if(event.key==='Enter')checkCoachPwd()" style="width:100%;background:#1a1a1a;border:1px solid #2c2c2c;color:#fff;padding:14px 18px;border-radius:12px;font-family:'Barlow',sans-serif;font-size:0.95rem;margin-bottom:12px;outline:none;" onfocus="this.style.borderColor='#D4AF37'" onblur="this.style.borderColor='#2c2c2c'">
      <div id="coachPwdError" style="color:#C0392B;font-size:0.8rem;margin-bottom:12px;display:none;">Mot de passe incorrect</div>
      <button onclick="checkCoachPwd()" style="width:100%;background:#D4AF37;color:#000;border:none;padding:15px;border-radius:100px;font-family:'Barlow',sans-serif;font-weight:700;font-size:0.9rem;letter-spacing:1px;text-transform:uppercase;cursor:pointer;transition:all 0.3s;" onmouseover="this.style.background='#F0D060'" onmouseout="this.style.background='#D4AF37'">Connexion</button>
      <button onclick="closeCoachOverlay()" style="margin-top:14px;background:transparent;border:none;color:#444;font-family:'Barlow',sans-serif;font-size:0.82rem;cursor:pointer;letter-spacing:1px;text-transform:uppercase;">Annuler</button>
    </div>
  </div>

  <!-- DASHBOARD SCREEN -->
  <div id="coachDashboard" style="display:none;min-height:100vh;padding:0;">

    <!-- TOP NAV -->
    <nav style="position:sticky;top:0;z-index:100;background:rgba(10,10,10,0.95);backdrop-filter:blur(10px);border-bottom:1px solid #1a1a1a;padding:0 32px;display:flex;align-items:center;justify-content:space-between;height:64px;">
      <div style="display:flex;align-items:center;gap:16px;">
        <div style="width:36px;height:36px;background:rgba(212,175,55,0.15);border:1px solid rgba(212,175,55,0.4);border-radius:10px;display:flex;align-items:center;justify-content:center;color:#D4AF37;font-size:0.9rem;"><i class="fas fa-chart-line"></i></div>
        <span style="font-family:'Bebas Neue',cursive;font-size:1.3rem;letter-spacing:2px;color:#fff;">DASHBOARD <span style="color:#D4AF37;">COACH</span></span>
      </div>
      <div style="display:flex;align-items:center;gap:12px;">
        <button onclick="showDashTab('candidates')" id="tabBtnCandidates" class="dash-tab-btn active-tab">Candidats</button>
        <button onclick="showDashTab('cv')" id="tabBtnCv" class="dash-tab-btn">Mon CV</button>
        <button onclick="showDashTab('slots')" id="tabBtnSlots" class="dash-tab-btn">Créneaux</button>
        <button onclick="closeCoachOverlay()" style="background:transparent;border:1px solid #2a2a2a;color:#666;padding:8px 16px;border-radius:100px;font-family:'Barlow',sans-serif;font-size:0.78rem;letter-spacing:1px;text-transform:uppercase;cursor:pointer;" onmouseover="this.style.borderColor='#C0392B';this.style.color='#C0392B'" onmouseout="this.style.borderColor='#2a2a2a';this.style.color='#666'"><i class="fas fa-sign-out-alt"></i> Quitter</button>
      </div>
    </nav>

    <!-- ── TAB CANDIDATS ── -->
    <div id="dashCandidates" class="dash-panel" style="padding:32px;max-width:1200px;margin:0 auto;">

      <!-- Stats row -->
      <div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(180px,1fr));gap:16px;margin-bottom:32px;">
        <div class="stat-card"><div class="stat-icon" style="color:#D4AF37;"><i class="fas fa-users"></i></div><div class="stat-val" id="statTotal">0</div><div class="stat-label">Inscriptions</div></div>
        <div class="stat-card"><div class="stat-icon" style="color:#4CAF50;"><i class="fas fa-check-circle"></i></div><div class="stat-val" id="statConfirmed">0</div><div class="stat-label">Confirmés</div></div>
        <div class="stat-card"><div class="stat-icon" style="color:#F0D060;"><i class="fas fa-clock"></i></div><div class="stat-val" id="statNew">0</div><div class="stat-label">Nouveaux</div></div>
        <div class="stat-card"><div class="stat-icon" style="color:#C0392B;"><i class="fas fa-times-circle"></i></div><div class="stat-val" id="statRefused">0</div><div class="stat-label">Refusés</div></div>
        <div class="stat-card"><div class="stat-icon" style="color:#9B59B6;"><i class="fas fa-euro-sign"></i></div><div class="stat-val" id="statRevenue">0€</div><div class="stat-label">CA potentiel</div></div>
      </div>

      <!-- Filter + Search -->
      <div style="display:flex;gap:12px;margin-bottom:24px;flex-wrap:wrap;align-items:center;">
        <input id="searchInput" type="text" placeholder="🔍 Rechercher un candidat..." oninput="renderCandidatesList()" style="flex:1;min-width:220px;background:#1a1a1a;border:1px solid #2c2c2c;color:#fff;padding:11px 16px;border-radius:100px;font-family:'Barlow',sans-serif;font-size:0.85rem;outline:none;" onfocus="this.style.borderColor='#D4AF37'" onblur="this.style.borderColor='#2c2c2c'">
        <select id="filterStatus" onchange="renderCandidatesList()" style="background:#1a1a1a;border:1px solid #2c2c2c;color:#aaa;padding:11px 16px;border-radius:100px;font-family:'Barlow',sans-serif;font-size:0.85rem;outline:none;cursor:pointer;">
          <option value="">Tous les statuts</option>
          <option value="Nouveau">Nouveau</option>
          <option value="Confirmé">Confirmé</option>
          <option value="En attente">En attente</option>
          <option value="Refusé">Refusé</option>
        </select>
        <button onclick="exportCSV()" style="background:transparent;border:1px solid #2c2c2c;color:#888;padding:11px 18px;border-radius:100px;font-family:'Barlow',sans-serif;font-size:0.82rem;cursor:pointer;letter-spacing:1px;text-transform:uppercase;" onmouseover="this.style.borderColor='#D4AF37';this.style.color='#D4AF37'" onmouseout="this.style.borderColor='#2c2c2c';this.style.color='#888'"><i class="fas fa-download"></i> Export CSV</button>
        <button onclick="clearAllData()" style="background:transparent;border:1px solid #2c2c2c;color:#555;padding:11px 18px;border-radius:100px;font-family:'Barlow',sans-serif;font-size:0.82rem;cursor:pointer;letter-spacing:1px;text-transform:uppercase;" onmouseover="this.style.borderColor='#C0392B';this.style.color='#C0392B'" onmouseout="this.style.borderColor='#2c2c2c';this.style.color='#555'"><i class="fas fa-trash"></i> Vider</button>
      </div>

      <!-- List -->
      <div id="candidatesList"></div>
    </div>

    <!-- ── TAB CV ── -->
    <div id="dashCv" class="dash-panel" style="display:none;padding:32px;max-width:900px;margin:0 auto;">
      <div style="margin-bottom:32px;">
        <div style="font-family:'Bebas Neue',cursive;font-size:2rem;letter-spacing:2px;color:#fff;margin-bottom:6px;">ÉDITER MON <span style="color:#D4AF37;">CV PUBLIC</span></div>
        <p style="color:#555;font-size:0.88rem;">Les modifications s'affichent immédiatement sur la page publique</p>
      </div>

      <div style="display:grid;gap:20px;">
        <!-- Identité -->
        <div class="cv-section-card">
          <div class="cv-section-title"><i class="fas fa-user"></i> Identité</div>
          <div style="display:grid;grid-template-columns:1fr 1fr;gap:16px;margin-top:16px;">
            <div><label class="cv-label">Prénom</label><input id="cvPrenom" class="cv-input" value="MEHDI"></div>
            <div><label class="cv-label">Nom</label><input id="cvNom" class="cv-input" value="SARKHI"></div>
          </div>
          <div style="margin-top:16px;"><label class="cv-label">Badge / Statut</label><input id="cvBadge" class="cv-input" value="Coach Certifié · Wushu · Fitness"></div>
          <div style="margin-top:16px;"><label class="cv-label">Tagline / Accroche</label><input id="cvTagline" class="cv-input" value="Transforme ton corps. Libère ton potentiel."></div>
        </div>

        <!-- Credentials pills -->
        <div class="cv-section-card">
          <div class="cv-section-title"><i class="fas fa-medal"></i> Credentials (pills affichés sous le nom)</div>
          <div id="credsList" style="margin-top:16px;display:flex;flex-direction:column;gap:10px;"></div>
          <button onclick="addCred()" style="margin-top:14px;background:transparent;border:1px dashed #2c2c2c;color:#555;padding:10px;width:100%;border-radius:10px;font-family:'Barlow',sans-serif;font-size:0.82rem;cursor:pointer;letter-spacing:1px;text-transform:uppercase;" onmouseover="this.style.borderColor='#D4AF37';this.style.color='#D4AF37'" onmouseout="this.style.borderColor='#2c2c2c';this.style.color='#555'"><i class="fas fa-plus"></i> Ajouter un credential</button>
        </div>

        <!-- Packs prices -->
        <div class="cv-section-card">
          <div class="cv-section-title"><i class="fas fa-tag"></i> Prix des packs</div>
          <div id="packPricesList" style="margin-top:16px;display:flex;flex-direction:column;gap:12px;"></div>
        </div>

        <!-- Contact info -->
        <div class="cv-section-card">
          <div class="cv-section-title"><i class="fas fa-phone"></i> Informations de contact</div>
          <div style="display:grid;grid-template-columns:1fr 1fr;gap:16px;margin-top:16px;">
            <div><label class="cv-label">Téléphone</label><input id="cvTel" class="cv-input" value="+33 6 XX XX XX XX"></div>
            <div><label class="cv-label">Email</label><input id="cvEmail" class="cv-input" value="contact@mehdisarkhi.com"></div>
          </div>
          <div style="margin-top:16px;"><label class="cv-label">Localisation</label><input id="cvLoc" class="cv-input" value="Paris & Île-de-France"></div>
        </div>

        <button onclick="saveCVChanges()" style="background:#D4AF37;color:#000;border:none;padding:16px;border-radius:100px;font-family:'Barlow',sans-serif;font-weight:700;font-size:0.95rem;letter-spacing:1px;text-transform:uppercase;cursor:pointer;width:100%;transition:all 0.3s;" onmouseover="this.style.background='#F0D060'" onmouseout="this.style.background='#D4AF37'"><i class="fas fa-save"></i> Sauvegarder & Appliquer</button>
      </div>
    </div>

    <!-- ── TAB CRÉNEAUX ── -->
    <div id="dashSlots" class="dash-panel" style="display:none;padding:32px;max-width:900px;margin:0 auto;">
      <div style="margin-bottom:32px;">
        <div style="font-family:'Bebas Neue',cursive;font-size:2rem;letter-spacing:2px;color:#fff;margin-bottom:6px;">GÉRER LES <span style="color:#D4AF37;">CRÉNEAUX</span></div>
        <p style="color:#555;font-size:0.88rem;">Activez / désactivez les créneaux visibles par les candidats</p>
      </div>
      <div id="slotsManagerGrid" style="display:grid;gap:24px;"></div>
      <button onclick="saveSlots()" style="margin-top:24px;background:#D4AF37;color:#000;border:none;padding:16px;border-radius:100px;font-family:'Barlow',sans-serif;font-weight:700;font-size:0.95rem;letter-spacing:1px;text-transform:uppercase;cursor:pointer;width:100%;transition:all 0.3s;" onmouseover="this.style.background='#F0D060'" onmouseout="this.style.background='#D4AF37'"><i class="fas fa-save"></i> Sauvegarder les créneaux</button>
    </div>

  </div><!-- /coachDashboard -->
</div><!-- /coachOverlay -->

<!-- DETAIL MODAL -->
<div id="candidateModal" style="display:none;position:fixed;inset:0;background:rgba(0,0,0,0.92);z-index:10000;overflow-y:auto;padding:40px 20px;">
  <div style="max-width:760px;margin:0 auto;background:#111;border:1px solid #222;border-radius:24px;overflow:hidden;">
    <div style="background:#1a1a1a;padding:20px 28px;display:flex;align-items:center;justify-content:space-between;border-bottom:1px solid #222;">
      <span style="font-family:'Bebas Neue',cursive;font-size:1.4rem;letter-spacing:2px;color:#D4AF37;">FICHE CANDIDAT</span>
      <button onclick="closeCandidateModal()" style="background:transparent;border:none;color:#555;font-size:1.2rem;cursor:pointer;" onmouseover="this.style.color='#fff'" onmouseout="this.style.color='#555'"><i class="fas fa-times"></i></button>
    </div>
    <div id="candidateModalContent" style="padding:28px;"></div>
  </div>
</div>

<style>
/* ─── DASHBOARD STYLES ─── */
.dash-tab-btn {
  background: transparent;
  border: 1px solid #2a2a2a;
  color: #555;
  padding: 8px 18px;
  border-radius: 100px;
  font-family: 'Barlow', sans-serif;
  font-size: 0.78rem;
  letter-spacing: 1px;
  text-transform: uppercase;
  cursor: pointer;
  transition: all 0.3s;
}
.dash-tab-btn:hover { border-color: #D4AF37; color: #D4AF37; }
.dash-tab-btn.active-tab { background: #D4AF37; color: #000; border-color: #D4AF37; }

.stat-card {
  background: #111;
  border: 1px solid #1e1e1e;
  border-radius: 16px;
  padding: 24px 20px;
  text-align: center;
  transition: border-color 0.3s;
}
.stat-card:hover { border-color: #2c2c2c; }
.stat-icon { font-size: 1.4rem; margin-bottom: 10px; }
.stat-val { font-family: 'Bebas Neue', cursive; font-size: 2.2rem; color: #fff; line-height: 1; }
.stat-label { font-size: 0.72rem; color: #444; text-transform: uppercase; letter-spacing: 2px; margin-top: 4px; }

.candidate-row {
  background: #111;
  border: 1px solid #1e1e1e;
  border-radius: 14px;
  padding: 18px 22px;
  display: flex;
  align-items: center;
  gap: 16px;
  margin-bottom: 10px;
  cursor: pointer;
  transition: all 0.25s;
}
.candidate-row:hover { border-color: #D4AF37; transform: translateX(4px); }
.candidate-avatar { width: 42px; height: 42px; background: rgba(212,175,55,0.12); border: 1px solid rgba(212,175,55,0.25); border-radius: 50%; display: flex; align-items: center; justify-content: center; color: #D4AF37; font-size: 0.9rem; flex-shrink: 0; }
.candidate-info { flex: 1; }
.candidate-name { font-weight: 700; font-size: 0.95rem; color: #fff; }
.candidate-meta { font-size: 0.78rem; color: #555; margin-top: 2px; }
.candidate-pack { font-size: 0.78rem; color: #D4AF37; font-weight: 600; }
.status-badge { padding: 4px 12px; border-radius: 100px; font-size: 0.7rem; font-weight: 700; letter-spacing: 1px; text-transform: uppercase; }
.status-Nouveau { background: rgba(240,208,96,0.12); color: #F0D060; }
.status-Confirmé { background: rgba(76,175,80,0.12); color: #4CAF50; }
.status-En.attente { background: rgba(100,100,200,0.12); color: #7777dd; }
.status-Refusé { background: rgba(192,57,43,0.12); color: #C0392B; }

.cv-section-card { background: #111; border: 1px solid #1e1e1e; border-radius: 16px; padding: 24px; }
.cv-section-title { font-size: 0.78rem; font-weight: 700; letter-spacing: 2px; text-transform: uppercase; color: #D4AF37; display: flex; align-items: center; gap: 8px; }
.cv-label { font-size: 0.7rem; font-weight: 700; letter-spacing: 1.5px; text-transform: uppercase; color: #555; display: block; margin-bottom: 6px; }
.cv-input { width: 100%; background: #1a1a1a; border: 1px solid #2c2c2c; color: #fff; padding: 11px 14px; border-radius: 10px; font-family: 'Barlow', sans-serif; font-size: 0.88rem; outline: none; transition: border-color 0.3s; }
.cv-input:focus { border-color: #D4AF37; }

.cred-edit-row { display: flex; gap: 8px; align-items: center; }
.cred-edit-row .cv-input { flex: 1; }
.cred-edit-row button { background: transparent; border: 1px solid #2c2c2c; color: #C0392B; width: 36px; height: 36px; border-radius: 8px; cursor: pointer; font-size: 0.9rem; transition: all 0.2s; flex-shrink: 0; }
.cred-edit-row button:hover { background: rgba(192,57,43,0.1); border-color: #C0392B; }

.slot-day-section { background: #111; border: 1px solid #1e1e1e; border-radius: 14px; padding: 20px; }
.slot-day-title { font-family: 'Bebas Neue', cursive; font-size: 1.2rem; letter-spacing: 2px; color: #D4AF37; margin-bottom: 14px; text-transform: capitalize; }
.slot-toggle-row { display: flex; align-items: center; gap: 12px; padding: 10px 14px; background: #1a1a1a; border-radius: 10px; margin-bottom: 8px; cursor: pointer; transition: background 0.2s; }
.slot-toggle-row:hover { background: #222; }
.slot-toggle { width: 40px; height: 22px; border-radius: 100px; position: relative; cursor: pointer; flex-shrink: 0; transition: background 0.3s; }
.slot-toggle.on { background: #D4AF37; }
.slot-toggle.off { background: #2c2c2c; }
.slot-toggle::after { content: ''; position: absolute; top: 3px; width: 16px; height: 16px; background: #fff; border-radius: 50%; transition: left 0.3s; }
.slot-toggle.on::after { left: 21px; }
.slot-toggle.off::after { left: 3px; }
.slot-label { flex: 1; font-size: 0.85rem; color: #ccc; font-weight: 500; }
.slot-type-label { font-size: 0.72rem; color: #555; letter-spacing: 1px; }

.modal-detail-section { margin-bottom: 24px; padding-bottom: 24px; border-bottom: 1px solid #1e1e1e; }
.modal-detail-section:last-child { border-bottom: none; }
.modal-section-title { font-size: 0.72rem; font-weight: 700; letter-spacing: 2px; text-transform: uppercase; color: #D4AF37; margin-bottom: 14px; }
.modal-row { display: flex; justify-content: space-between; padding: 7px 0; font-size: 0.85rem; border-bottom: 1px solid #1a1a1a; }
.modal-row:last-child { border: none; }
.modal-row .mk { color: #555; }
.modal-row .mv { color: #ccc; font-weight: 500; text-align: right; max-width: 60%; }

.status-select { background: #1a1a1a; border: 1px solid #2c2c2c; color: #D4AF37; padding: 8px 14px; border-radius: 100px; font-family: 'Barlow', sans-serif; font-size: 0.82rem; font-weight: 700; cursor: pointer; outline: none; }

.toast-msg { position: fixed; bottom: 32px; right: 32px; background: #111; border: 1px solid #D4AF37; color: #D4AF37; padding: 14px 24px; border-radius: 12px; font-family: 'Barlow', sans-serif; font-size: 0.88rem; font-weight: 600; z-index: 99999; transform: translateY(80px); opacity: 0; transition: all 0.4s cubic-bezier(0.175,0.885,0.32,1.275); }
.toast-msg.show { transform: translateY(0); opacity: 1; }
</style>
<div class="toast-msg" id="toastMsg"></div>

<!-- SELECTED PACK BANNER -->
<div class="selected-pack-banner" id="packBanner" onclick="scrollToForm()">
  <i class="fas fa-star"></i>
  <span id="packBannerText">Pack sélectionné → S'inscrire</span>
  <i class="fas fa-arrow-right"></i>
</div>

<script>
// ───── DATA ─────
const STATE = {
  selectedPack: null,
  selectedDay: 'lundi',
  selectedSlot: null,
  currentStep: 1,
  files: { photo: null, certif: null }
};

const SCHEDULE = {
  lundi:    [
    { time:'07:00', type:'Fitness', available:true },
    { time:'09:00', type:'Wushu', available:true },
    { time:'12:00', type:'Sanda', available:false },
    { time:'17:00', type:'Fitness', available:true },
    { time:'18:30', type:'Kung Fu', available:true },
    { time:'20:00', type:'Cardio', available:false },
  ],
  mardi:    [
    { time:'08:00', type:'Remise en forme', available:true },
    { time:'10:00', type:'Fitness', available:true },
    { time:'18:00', type:'Wushu', available:true },
    { time:'19:30', type:'Sanda', available:true },
  ],
  mercredi: [
    { time:'09:00', type:'Kung Fu', available:true },
    { time:'11:00', type:'Fitness', available:false },
    { time:'14:00', type:'Souplesse', available:true },
    { time:'17:00', type:'Cardio', available:true },
    { time:'19:00', type:'Wushu', available:true },
  ],
  jeudi:    [
    { time:'07:30', type:'Fitness', available:true },
    { time:'09:00', type:'Sanda', available:true },
    { time:'18:00', type:'Kung Fu', available:false },
    { time:'19:30', type:'Remise en forme', available:true },
  ],
  vendredi: [
    { time:'08:00', type:'Wushu', available:true },
    { time:'10:00', type:'Fitness', available:true },
    { time:'17:30', type:'Cardio', available:true },
    { time:'19:00', type:'Sanda', available:false },
  ],
  samedi:   [
    { time:'09:00', type:'Fitness', available:true },
    { time:'10:30', type:'Kung Fu', available:true },
    { time:'12:00', type:'Wushu', available:true },
    { time:'14:00', type:'Sanda', available:true },
  ]
};

// ───── PARTICLES ─────
function createParticles() {
  const container = document.getElementById('particles');
  for(let i = 0; i < 20; i++) {
    const p = document.createElement('div');
    p.className = 'particle';
    p.style.cssText = `left:${Math.random()*100}%; top:${Math.random()*100}%; --dur:${3+Math.random()*5}s; --delay:${Math.random()*4}s;`;
    container.appendChild(p);
  }
}

// ───── PACKS ─────
function selectPack(card) {
  document.querySelectorAll('.pack-card').forEach(c => c.classList.remove('selected'));
  card.classList.add('selected');
  STATE.selectedPack = {
    name: card.dataset.pack,
    sessions: card.dataset.sessions,
    price: card.dataset.price,
    validity: card.dataset.validity
  };
  const banner = document.getElementById('packBanner');
  document.getElementById('packBannerText').textContent = `Pack ${STATE.selectedPack.name} – ${STATE.selectedPack.price}€ sélectionné`;
  banner.classList.add('visible');
  setTimeout(() => banner.classList.add('visible'), 50);
}

function scrollToForm() {
  document.getElementById('inscription').scrollIntoView({ behavior: 'smooth' });
}

// ───── HORAIRES ─────
function selectDay(btn) {
  document.querySelectorAll('.day-tab').forEach(t => t.classList.remove('active'));
  btn.classList.add('active');
  STATE.selectedDay = btn.dataset.day;
  STATE.selectedSlot = null;
  renderSlots();
}

function renderSlots() {
  const slots = SCHEDULE[STATE.selectedDay];
  const container = document.getElementById('timeSlots');
  container.innerHTML = slots.map(s => `
    <div class="time-slot ${s.available ? 'available' : 'unavailable'} ${STATE.selectedSlot?.time === s.time ? 'selected' : ''}"
         onclick="${s.available ? `selectSlot('${s.time}','${s.type}')` : ''}">
      <div class="selected-check"><i class="fas fa-check"></i></div>
      <div class="time">${s.time}</div>
      <div class="type">${s.type}</div>
      <div class="spots">${s.available ? '✓ Disponible' : '✗ Complet'}</div>
    </div>
  `).join('');
}

function selectSlot(time, type) {
  STATE.selectedSlot = { time, type, day: STATE.selectedDay };
  renderSlots();
}

// ───── FORM STEPS ─────
function goToStep(n) {
  if (n > STATE.currentStep) return; // only go back
  STATE.currentStep = n;
  updateStepUI();
}

function nextStep(from) {
  if (!validateStep(from)) return;
  STATE.currentStep = from + 1;
  if (STATE.currentStep === 6) buildSummary();
  updateStepUI();
  document.getElementById('inscription').scrollIntoView({ behavior: 'smooth', block: 'start' });
}

function prevStep(from) {
  STATE.currentStep = from - 1;
  updateStepUI();
  document.getElementById('inscription').scrollIntoView({ behavior: 'smooth', block: 'start' });
}

function updateStepUI() {
  document.querySelectorAll('.form-step').forEach(s => s.classList.remove('active'));
  document.getElementById('step' + STATE.currentStep).classList.add('active');
  document.querySelectorAll('.progress-step').forEach(p => {
    const n = parseInt(p.dataset.step);
    p.classList.remove('active', 'done');
    if (n === STATE.currentStep) p.classList.add('active');
    else if (n < STATE.currentStep) p.classList.add('done');
  });
}

function validateStep(n) {
  if (n === 1) {
    const nom = document.getElementById('fNom').value.trim();
    const prenom = document.getElementById('fPrenom').value.trim();
    const tel = document.getElementById('fTel').value.trim();
    const email = document.getElementById('fEmail').value.trim();
    const motiv = document.getElementById('fMotivations').value.trim();
    if (!nom || !prenom || !tel || !email || !motiv) {
      flashError('Veuillez remplir tous les champs obligatoires (*)');
      return false;
    }
  }
  if (n === 5) {
    if (!STATE.files.photo) {
      flashError('La photo d\'identité est obligatoire.');
      return false;
    }
  }
  return true;
}

function flashError(msg) {
  const el = document.createElement('div');
  el.style.cssText = 'position:fixed;top:20px;right:20px;background:#C0392B;color:white;padding:14px 22px;border-radius:12px;font-weight:700;font-size:0.85rem;z-index:9999;box-shadow:0 10px 30px rgba(192,57,43,0.5);';
  el.innerHTML = '<i class="fas fa-exclamation-triangle"></i> ' + msg;
  document.body.appendChild(el);
  setTimeout(() => el.remove(), 3500);
}

// ───── HEALTH RADIOS ─────
function setupHealthToggles() {
  const pairs = [
    ['hStress','detailStress'],['hMig','detailMig'],['hCar','detailCar'],
    ['hResp','detailResp'],['hCirc','detailCirc'],['hDos','detailDos'],
    ['hOsteo','detailOsteo'],['hMusc','detailMusc'],['hAll','detailAll'],
    ['hGross','detailGross']
  ];
  pairs.forEach(([name, detailId]) => {
    document.querySelectorAll(`input[name="${name}"]`).forEach(r => {
      r.addEventListener('change', () => {
        const detail = document.getElementById(detailId);
        if (r.value === 'oui') detail.classList.add('visible');
        else detail.classList.remove('visible');
      });
    });
  });
}

// ───── IMC CALC ─────
function setupIMCCalc() {
  ['fPoidsActuel','fTaille'].forEach(id => {
    document.getElementById(id).addEventListener('input', calcIMC);
  });
  ['fFC0','fFC1','fFC2'].forEach(id => {
    document.getElementById(id).addEventListener('input', calcRuffier);
  });
}

function calcIMC() {
  const p = parseFloat(document.getElementById('fPoidsActuel').value);
  const t = parseFloat(document.getElementById('fTaille').value) / 100;
  if (p && t) {
    const imc = (p / (t * t)).toFixed(1);
    document.getElementById('fIMC').value = imc + ' – ' + getIMCLabel(parseFloat(imc));
  }
}

function getIMCLabel(imc) {
  if (imc < 18.5) return 'Insuffisance pondérale';
  if (imc < 25) return 'Poids normal ✓';
  if (imc < 30) return 'Surpoids';
  return 'Obésité';
}

function calcRuffier() {
  const fc0 = parseFloat(document.getElementById('fFC0').value);
  const fc1 = parseFloat(document.getElementById('fFC1').value);
  const fc2 = parseFloat(document.getElementById('fFC2').value);
  if (fc0 && fc1 && fc2) {
    const r = ((fc0 + fc1 + fc2 - 200) / 10).toFixed(1);
    const label = r <= 0 ? '⭐ Excellent' : r <= 5 ? 'Fort' : r <= 10 ? 'Bon' : r <= 15 ? 'Moyen' : 'Faible';
    document.getElementById('fRuffier').value = r + ' – ' + label;
  }
}

// ───── FILE UPLOAD ─────
function handleFileUpload(type, input) {
  const file = input.files[0];
  if (!file) return;
  STATE.files[type] = file;
  if (type === 'photo') {
    const reader = new FileReader();
    reader.onload = (e) => {
      document.getElementById('photoThumb').src = e.target.result;
      document.getElementById('photoName').textContent = file.name;
      document.getElementById('photoSize').textContent = (file.size / 1024 / 1024).toFixed(2) + ' MB';
      document.getElementById('photoPreview').classList.add('visible');
      document.getElementById('photoArea').style.display = 'none';
    };
    reader.readAsDataURL(file);
  } else {
    document.getElementById('certifName').textContent = file.name;
    document.getElementById('certifSize').textContent = (file.size / 1024 / 1024).toFixed(2) + ' MB';
    document.getElementById('certifPreview').classList.add('visible');
    document.getElementById('certifArea').style.display = 'none';
  }
}

// ───── SUMMARY ─────
function buildSummary() {
  const nom = document.getElementById('fNom').value;
  const prenom = document.getElementById('fPrenom').value;
  const ddn = document.getElementById('fDdn').value;
  const tel = document.getElementById('fTel').value;
  const email = document.getElementById('fEmail').value;
  const motiv = document.getElementById('fMotivations').value;
  const dispos = [...document.querySelectorAll('input[name="dispo"]:checked')].map(i => i.value).join(', ');
  const pack = STATE.selectedPack;
  const slot = STATE.selectedSlot;
  const poids = document.getElementById('fPoidsActuel').value;
  const taille = document.getElementById('fTaille').value;
  const imc = document.getElementById('fIMC').value;

  document.getElementById('summaryBox').innerHTML = `
    <div class="summary-section">
      <h4>Coordonnées</h4>
      <div class="summary-row"><span class="key">Nom complet</span><span class="val">${prenom} ${nom}</span></div>
      <div class="summary-row"><span class="key">Date de naissance</span><span class="val">${ddn || '—'}</span></div>
      <div class="summary-row"><span class="key">Téléphone</span><span class="val">${tel}</span></div>
      <div class="summary-row"><span class="key">Email</span><span class="val">${email}</span></div>
      <div class="summary-row"><span class="key">Motivations</span><span class="val">${motiv.substring(0,60)}...</span></div>
      <div class="summary-row"><span class="key">Disponibilités</span><span class="val">${dispos || '—'}</span></div>
    </div>
    <div class="summary-section">
      <h4>Pack choisi</h4>
      ${pack ? `
        <div class="summary-row"><span class="key">Pack</span><span class="val highlight">${pack.name}</span></div>
        <div class="summary-row"><span class="key">Prix</span><span class="val highlight">${pack.price}€</span></div>
        <div class="summary-row"><span class="key">Validité</span><span class="val">${pack.validity} jours</span></div>
      ` : '<div style="color:#888;font-size:0.85rem">⚠️ Aucun pack sélectionné</div>'}
    </div>
    <div class="summary-section">
      <h4>Créneau préféré</h4>
      ${slot ? `
        <div class="summary-row"><span class="key">Jour</span><span class="val">${slot.day.charAt(0).toUpperCase()+slot.day.slice(1)}</span></div>
        <div class="summary-row"><span class="key">Heure</span><span class="val">${slot.time}</span></div>
        <div class="summary-row"><span class="key">Type de séance</span><span class="val">${slot.type}</span></div>
      ` : '<div style="color:#888;font-size:0.85rem">Aucun créneau sélectionné (à confirmer)</div>'}
    </div>
    <div class="summary-section">
      <h4>Biométrie</h4>
      <div class="summary-row"><span class="key">Poids</span><span class="val">${poids || '—'} kg</span></div>
      <div class="summary-row"><span class="key">Taille</span><span class="val">${taille || '—'} cm</span></div>
      <div class="summary-row"><span class="key">IMC</span><span class="val">${imc || '—'}</span></div>
    </div>
    <div class="summary-section">
      <h4>Documents</h4>
      <div class="summary-row"><span class="key">Photo d'identité</span><span class="val" style="color:${STATE.files.photo ? '#4CAF50' : '#C0392B'}">${STATE.files.photo ? '✓ Fournie' : '✗ Manquante'}</span></div>
      <div class="summary-row"><span class="key">Certificat médical</span><span class="val" style="color:${STATE.files.certif ? '#4CAF50' : '#D4AF37'}">${STATE.files.certif ? '✓ Fourni' : '⏳ À fournir (1 mois)'}</span></div>
    </div>
  `;
}

// ───── SUBMIT ─────
function submitForm() {
  const cgvCheckbox = document.getElementById('fCGV');
  if (!cgvCheckbox || !cgvCheckbox.checked) {
    flashError('Veuillez accepter les conditions générales.');
    return;
  }

  // Build payload
  const payload = gatherAllData();
  
  // Save to localStorage (admin will read it)
  const clients = JSON.parse(localStorage.getItem('mehdi_clients') || '[]');
  clients.push(payload);
  localStorage.setItem('mehdi_clients', JSON.stringify(clients));

  // Show success
  document.querySelector('.form-body').style.display = 'none';
  document.querySelector('.form-progress').style.display = 'none';
  document.getElementById('successScreen').classList.add('visible');
  document.getElementById('packBanner').classList.remove('visible');
}

function gatherAllData() {
  return {
    id: 'C' + Date.now(),
    createdAt: new Date().toISOString(),
    status: 'Nouveau',
    certificatMedical: STATE.files.certif ? 'Fourni' : 'À fournir',
    // Identité
    nom: document.getElementById('fNom').value,
    prenom: document.getElementById('fPrenom').value,
    ddn: document.getElementById('fDdn').value,
    ldn: document.getElementById('fLdn').value,
    tel: document.getElementById('fTel').value,
    email: document.getElementById('fEmail').value,
    adresse: document.getElementById('fAdresse').value,
    motivations: document.getElementById('fMotivations').value,
    disponibilites: [...document.querySelectorAll('input[name="dispo"]:checked')].map(i=>i.value).join(', '),
    // Pack & Slot
    packChoisi: STATE.selectedPack,
    creneauPreferred: STATE.selectedSlot,
    // Santé
    sante: {
      stress: document.querySelector('input[name="hStress"]:checked')?.value,
      stressDetail: document.getElementById('fStressDetail').value,
      insomnies: document.querySelector('input[name="hInsom"]:checked')?.value,
      migraines: document.querySelector('input[name="hMig"]:checked')?.value,
      migDetail: document.getElementById('fMigDetail').value,
      cardiaque: document.querySelector('input[name="hCar"]:checked')?.value,
      carDetail: document.getElementById('fCarDetail').value,
      respiratoire: document.querySelector('input[name="hResp"]:checked')?.value,
      respDetail: document.getElementById('fRespDetail').value,
      circulatoire: document.querySelector('input[name="hCirc"]:checked')?.value,
      circDetail: document.getElementById('fCircDetail').value,
      dos: document.querySelector('input[name="hDos"]:checked')?.value,
      dosDetail: document.getElementById('fDosDetail').value,
      osteo: document.querySelector('input[name="hOsteo"]:checked')?.value,
      osteoDetail: document.getElementById('fOsteoDetail').value,
      musculo: document.querySelector('input[name="hMusc"]:checked')?.value,
      muscDetail: document.getElementById('fMuscDetail').value,
      allergies: document.querySelector('input[name="hAll"]:checked')?.value,
      allerDetail: document.getElementById('fAllDetail').value,
      grossesse: document.querySelector('input[name="hGross"]:checked')?.value,
      grossDetail: document.getElementById('fGrossDetail').value,
      fumeur: document.querySelector('input[name="hFum"]:checked')?.value,
      traitement: document.getElementById('fTraitement').value,
      autres: document.getElementById('fAutresSante').value
    },
    // Sport
    sport: {
      pratique: document.querySelector('input[name="fSport"]:checked')?.value,
      nbSeances: document.getElementById('fNbSeances').value,
      duree: document.getElementById('fDureeSeances').value,
      passe: document.getElementById('fPasse').value,
      profession: document.getElementById('fProfession').value,
      heuresTravail: document.getElementById('fHeuresTravail').value,
      posture: document.querySelector('input[name="fPosQuot"]:checked')?.value,
      vieActive: document.querySelector('input[name="fVieActive"]:checked')?.value,
      equilibre: document.querySelector('input[name="fEquil"]:checked')?.value,
      alimentation: document.querySelector('input[name="fMange"]:checked')?.value,
      eau: document.querySelector('input[name="fEau"]:checked')?.value,
      cafe: document.getElementById('fCafe').value,
      the: document.getElementById('fThe').value,
      sucre: document.getElementById('fSucre').value
    },
    // Biométrie
    biometrie: {
      poidsActuel: document.getElementById('fPoidsActuel').value,
      poidsHaut: document.getElementById('fPoidsHaut').value,
      poidsDesire: document.getElementById('fPoidsDesire').value,
      taille: document.getElementById('fTaille').value,
      imc: document.getElementById('fIMC').value,
      masseGrasse: document.getElementById('fMasseGrasse').value,
      fc0: document.getElementById('fFC0').value,
      fc1: document.getElementById('fFC1').value,
      fc2: document.getElementById('fFC2').value,
      ruffier: document.getElementById('fRuffier').value,
      pompes: document.getElementById('fPompes').value,
      chaise: document.getElementById('fChaise').value,
      souplesse: document.querySelector('input[name="fSoup"]:checked')?.value,
      scapulaire: document.querySelector('input[name="fScap"]:checked')?.value
    }
  };
}

// ───── SCROLL ANIMATION ─────
const observer = new IntersectionObserver((entries) => {
  entries.forEach(e => { if(e.isIntersecting) e.target.classList.add('visible'); });
}, { threshold: 0.1 });

document.querySelectorAll('.animate-on-scroll').forEach(el => observer.observe(el));

// ═══════════════════════════════════════════════════
//   COACH DASHBOARD
// ═══════════════════════════════════════════════════
const COACH_PWD = 'mehdi2024'; // ← Changez ce mot de passe !

// ── DATA COACH (CV éditable) ──
let COACH_CV = JSON.parse(localStorage.getItem('mehdi_cv') || 'null') || {
  prenom: 'MEHDI', nom: 'SARKHI',
  badge: 'Coach Certifié · Wushu · Fitness',
  tagline: 'Transforme ton corps. Libère ton potentiel.',
  tel: '+33 6 XX XX XX XX',
  email: 'contact@mehdisarkhi.com',
  loc: 'Paris & Île-de-France',
  creds: [
    { icon: 'fa-fist-raised', text: '5ème Dan Wushu Kung Fu' },
    { icon: 'fa-dumbbell', text: 'Instructeur Fitness 2° Degré' },
    { icon: 'fa-trophy', text: 'Entraîneur Équipe Régionale Sanda' },
    { icon: 'fa-certificate', text: 'Coach Certifié STAPS' }
  ]
};

// ── SLOTS MANAGER ──
let SLOTS_STATE = JSON.parse(localStorage.getItem('mehdi_slots_state') || 'null');

// ── PACKS DATA (for editor) ──
window.PACKS_DATA = [
  { name: 'Découverte', sessions: 1, price: 50, validity: 30 },
  { name: 'Forme +', sessions: 5, price: 230, validity: 60 },
  { name: 'Performance', sessions: 10, price: 420, validity: 90 },
  { name: 'Illimité 1 mois', sessions: 0, price: 300, validity: 30 }
];

// ── OPEN / CLOSE ──
function openCoachLogin() {
  document.getElementById('coachOverlay').style.display = 'block';
  document.getElementById('coachLogin').style.display = 'flex';
  document.getElementById('coachDashboard').style.display = 'none';
  document.getElementById('coachPwdInput').value = '';
  document.getElementById('coachPwdError').style.display = 'none';
  setTimeout(() => document.getElementById('coachPwdInput').focus(), 100);
}

function closeCoachOverlay() {
  document.getElementById('coachOverlay').style.display = 'none';
}

function checkCoachPwd() {
  const val = document.getElementById('coachPwdInput').value;
  if (val === COACH_PWD) {
    document.getElementById('coachLogin').style.display = 'none';
    document.getElementById('coachDashboard').style.display = 'block';
    showDashTab('candidates');
  } else {
    document.getElementById('coachPwdError').style.display = 'block';
    document.getElementById('coachPwdInput').value = '';
    document.getElementById('coachPwdInput').focus();
  }
}

// ── TAB SWITCHING ──
function showDashTab(tab) {
  ['candidates','cv','slots'].forEach(t => {
    const capT = t.charAt(0).toUpperCase() + t.slice(1);
    document.getElementById('dash' + capT).style.display = 'none';
    document.getElementById('tabBtn' + capT).classList.remove('active-tab');
  });
  const capTab = tab.charAt(0).toUpperCase() + tab.slice(1);
  document.getElementById('dash' + capTab).style.display = 'block';
  document.getElementById('tabBtn' + capTab).classList.add('active-tab');

  if (tab === 'candidates') { updateStats(); renderCandidatesList(); }
  if (tab === 'cv') { renderCVEditor(); }
  if (tab === 'slots') { renderSlotsManager(); }
}

// ── STATS ──
function updateStats() {
  const clients = JSON.parse(localStorage.getItem('mehdi_clients') || '[]');
  document.getElementById('statTotal').textContent = clients.length;
  document.getElementById('statConfirmed').textContent = clients.filter(c => c.status === 'Confirmé').length;
  document.getElementById('statNew').textContent = clients.filter(c => c.status === 'Nouveau').length;
  document.getElementById('statRefused').textContent = clients.filter(c => c.status === 'Refusé').length;
  const rev = clients.reduce((sum, c) => sum + (parseInt(c.packChoisi?.price) || 0), 0);
  document.getElementById('statRevenue').textContent = rev + '€';
}

// ── CANDIDATES LIST ──
function renderCandidatesList() {
  const clients = JSON.parse(localStorage.getItem('mehdi_clients') || '[]');
  const search = document.getElementById('searchInput').value.toLowerCase();
  const filterSt = document.getElementById('filterStatus').value;
  const container = document.getElementById('candidatesList');

  const filtered = clients.filter(c => {
    const name = (c.prenom + ' ' + c.nom).toLowerCase();
    const matchSearch = !search || name.includes(search) || (c.email||'').toLowerCase().includes(search);
    const matchStatus = !filterSt || c.status === filterSt;
    return matchSearch && matchStatus;
  });

  if (filtered.length === 0) {
    container.innerHTML = '<div style="text-align:center;color:#333;padding:60px 0;font-size:0.88rem;letter-spacing:2px;text-transform:uppercase;">Aucun candidat trouvé</div>';
    return;
  }

  container.innerHTML = filtered.map((c, idx) => {
    const realIdx = clients.findIndex(x => x.id === c.id);
    const statusClass = 'status-' + (c.status || 'Nouveau').replace(' ', '.');
    const date = c.createdAt ? new Date(c.createdAt).toLocaleDateString('fr-FR') : '—';
    return `
      <div class="candidate-row" onclick="openCandidateModal(${realIdx})">
        <div class="candidate-avatar"><i class="fas fa-user"></i></div>
        <div class="candidate-info">
          <div class="candidate-name">${c.prenom || ''} ${c.nom || ''}</div>
          <div class="candidate-meta">${c.email || '—'} · ${c.tel || '—'} · <span style="color:#555">${date}</span></div>
        </div>
        <div style="text-align:right;flex-shrink:0;">
          <div class="candidate-pack">${c.packChoisi?.name || 'Pas de pack'}</div>
          <div class="candidate-meta" style="margin-top:4px;">${c.packChoisi?.price ? c.packChoisi.price + '€' : ''}</div>
        </div>
        <span class="status-badge ${statusClass}">${c.status || 'Nouveau'}</span>
      </div>
    `;
  }).join('');
}

// ── CANDIDATE MODAL ──
function openCandidateModal(idx) {
  const clients = JSON.parse(localStorage.getItem('mehdi_clients') || '[]');
  const c = clients[idx];
  if (!c) return;
  const date = c.createdAt ? new Date(c.createdAt).toLocaleString('fr-FR') : '—';

  document.getElementById('candidateModalContent').innerHTML = `
    <div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:24px;flex-wrap:wrap;gap:12px;">
      <div>
        <div style="font-family:'Bebas Neue',cursive;font-size:2rem;letter-spacing:2px;color:#fff;">${c.prenom || ''} <span style="color:#D4AF37">${c.nom || ''}</span></div>
        <div style="font-size:0.8rem;color:#444;margin-top:4px;">ID: ${c.id} · Inscrit le ${date}</div>
      </div>
      <div style="display:flex;align-items:center;gap:10px;">
        <label style="font-size:0.72rem;color:#555;text-transform:uppercase;letter-spacing:1px;">Statut</label>
        <select class="status-select" onchange="updateCandidateStatus(${idx}, this.value)">
          ${['Nouveau','Confirmé','En attente','Refusé'].map(s => `<option value="${s}" ${c.status===s?'selected':''}>${s}</option>`).join('')}
        </select>
      </div>
    </div>

    <div class="modal-detail-section">
      <div class="modal-section-title">Coordonnées</div>
      <div class="modal-row"><span class="mk">Email</span><span class="mv">${c.email||'—'}</span></div>
      <div class="modal-row"><span class="mk">Téléphone</span><span class="mv">${c.tel||'—'}</span></div>
      <div class="modal-row"><span class="mk">Date de naissance</span><span class="mv">${c.ddn||'—'}</span></div>
      <div class="modal-row"><span class="mk">Adresse</span><span class="mv">${c.adresse||'—'}</span></div>
      <div class="modal-row"><span class="mk">Disponibilités</span><span class="mv">${c.disponibilites||'—'}</span></div>
    </div>

    <div class="modal-detail-section">
      <div class="modal-section-title">Pack & Créneau</div>
      <div class="modal-row"><span class="mk">Pack choisi</span><span class="mv" style="color:#D4AF37">${c.packChoisi?.name||'Aucun'}</span></div>
      <div class="modal-row"><span class="mk">Prix</span><span class="mv">${c.packChoisi?.price ? c.packChoisi.price+'€' : '—'}</span></div>
      <div class="modal-row"><span class="mk">Créneau préféré</span><span class="mv">${c.creneauPreferred ? c.creneauPreferred.day+' '+c.creneauPreferred.time+' – '+c.creneauPreferred.type : '—'}</span></div>
    </div>

    <div class="modal-detail-section">
      <div class="modal-section-title">Biométrie</div>
      <div class="modal-row"><span class="mk">Poids actuel</span><span class="mv">${c.biometrie?.poidsActuel||'—'} kg</span></div>
      <div class="modal-row"><span class="mk">Taille</span><span class="mv">${c.biometrie?.taille||'—'} cm</span></div>
      <div class="modal-row"><span class="mk">IMC</span><span class="mv">${c.biometrie?.imc||'—'}</span></div>
      <div class="modal-row"><span class="mk">Poids désiré</span><span class="mv">${c.biometrie?.poidsDesire||'—'} kg</span></div>
      <div class="modal-row"><span class="mk">Ruffier</span><span class="mv">${c.biometrie?.ruffier||'—'}</span></div>
    </div>

    <div class="modal-detail-section">
      <div class="modal-section-title">Documents</div>
      <div class="modal-row"><span class="mk">Certificat médical</span><span class="mv" style="color:${c.certificatMedical==='Fourni'?'#4CAF50':'#D4AF37'}">${c.certificatMedical||'À fournir'}</span></div>
    </div>

    <div class="modal-detail-section">
      <div class="modal-section-title">Motivations</div>
      <p style="font-size:0.85rem;color:#aaa;line-height:1.6;">${c.motivations||'—'}</p>
    </div>

    <div style="display:flex;gap:12px;margin-top:8px;flex-wrap:wrap;">
      <a href="mailto:${c.email}" style="flex:1;background:rgba(212,175,55,0.1);border:1px solid rgba(212,175,55,0.3);color:#D4AF37;padding:12px;border-radius:10px;text-align:center;text-decoration:none;font-family:'Barlow',sans-serif;font-size:0.82rem;font-weight:700;letter-spacing:1px;text-transform:uppercase;min-width:120px;"><i class="fas fa-envelope"></i> Email</a>
      <a href="tel:${c.tel}" style="flex:1;background:rgba(76,175,80,0.08);border:1px solid rgba(76,175,80,0.25);color:#4CAF50;padding:12px;border-radius:10px;text-align:center;text-decoration:none;font-family:'Barlow',sans-serif;font-size:0.82rem;font-weight:700;letter-spacing:1px;text-transform:uppercase;min-width:120px;"><i class="fas fa-phone"></i> Appeler</a>
      <button onclick="deleteCandidate(${idx})" style="flex:1;background:rgba(192,57,43,0.08);border:1px solid rgba(192,57,43,0.25);color:#C0392B;padding:12px;border-radius:10px;font-family:'Barlow',sans-serif;font-size:0.82rem;font-weight:700;letter-spacing:1px;text-transform:uppercase;cursor:pointer;min-width:120px;"><i class="fas fa-trash"></i> Supprimer</button>
    </div>
  `;
  document.getElementById('candidateModal').style.display = 'block';
}

function closeCandidateModal() {
  document.getElementById('candidateModal').style.display = 'none';
  renderCandidatesList();
  updateStats();
}

function updateCandidateStatus(idx, newStatus) {
  const clients = JSON.parse(localStorage.getItem('mehdi_clients') || '[]');
  clients[idx].status = newStatus;
  localStorage.setItem('mehdi_clients', JSON.stringify(clients));
  showToast('Statut mis à jour : ' + newStatus);
}

function deleteCandidate(idx) {
  if (!confirm('Supprimer définitivement ce candidat ?')) return;
  const clients = JSON.parse(localStorage.getItem('mehdi_clients') || '[]');
  clients.splice(idx, 1);
  localStorage.setItem('mehdi_clients', JSON.stringify(clients));
  closeCandidateModal();
  showToast('Candidat supprimé');
}

function clearAllData() {
  if (!confirm('Vider TOUTES les inscriptions ? Cette action est irréversible.')) return;
  localStorage.removeItem('mehdi_clients');
  renderCandidatesList();
  updateStats();
  showToast('Données effacées');
}

function exportCSV() {
  const clients = JSON.parse(localStorage.getItem('mehdi_clients') || '[]');
  if (!clients.length) { showToast('Aucune donnée à exporter'); return; }
  const headers = ['ID','Date','Prénom','Nom','Email','Tel','Pack','Prix','Statut','IMC'];
  const rows = clients.map(c => [
    c.id, c.createdAt ? new Date(c.createdAt).toLocaleDateString('fr-FR') : '',
    c.prenom, c.nom, c.email, c.tel,
    c.packChoisi?.name||'', c.packChoisi?.price||'',
    c.status||'Nouveau', c.biometrie?.imc||''
  ].map(v => `"${String(v||'').replace(/"/g,'""')}"`).join(','));
  const csv = [headers.join(','), ...rows].join('\n');
  const a = document.createElement('a');
  a.href = 'data:text/csv;charset=utf-8,' + encodeURIComponent('\uFEFF' + csv);
  a.download = 'inscriptions_mehdi_' + new Date().toISOString().slice(0,10) + '.csv';
  a.click();
  showToast('Export CSV téléchargé !');
}

// ── CV EDITOR ──
function renderCVEditor() {
  document.getElementById('cvPrenom').value = COACH_CV.prenom;
  document.getElementById('cvNom').value = COACH_CV.nom;
  document.getElementById('cvBadge').value = COACH_CV.badge;
  document.getElementById('cvTagline').value = COACH_CV.tagline;
  document.getElementById('cvTel').value = COACH_CV.tel;
  document.getElementById('cvEmail').value = COACH_CV.email;
  document.getElementById('cvLoc').value = COACH_CV.loc;
  renderCredsList();
  renderPackPrices();
}

function renderCredsList() {
  const container = document.getElementById('credsList');
  container.innerHTML = COACH_CV.creds.map((cr, i) => `
    <div class="cred-edit-row">
      <input class="cv-input" style="width:100px;flex:0 0 100px;" placeholder="Icône FA" value="${cr.icon}" onchange="COACH_CV.creds[${i}].icon=this.value">
      <input class="cv-input" placeholder="Texte du credential" value="${cr.text}" onchange="COACH_CV.creds[${i}].text=this.value">
      <button onclick="removeCred(${i})"><i class="fas fa-times"></i></button>
    </div>
  `).join('');
}

function addCred() {
  COACH_CV.creds.push({ icon: 'fa-star', text: 'Nouveau credential' });
  renderCredsList();
}

function removeCred(i) {
  COACH_CV.creds.splice(i, 1);
  renderCredsList();
}

function renderPackPrices() {
  const container = document.getElementById('packPricesList');
  const packs = window.PACKS_DATA || [];
  if (!packs.length) {
    container.innerHTML = '<p style="color:#444;font-size:0.82rem;">Les packs sont définis dans le code source.</p>';
    return;
  }
  container.innerHTML = packs.map((p, i) => `
    <div style="display:flex;align-items:center;gap:12px;">
      <span style="flex:1;color:#ccc;font-size:0.88rem;">${p.name}</span>
      <input class="cv-input" style="width:120px;" type="number" value="${p.price}" onchange="window.PACKS_DATA[${i}].price=+this.value">
      <span style="color:#555;font-size:0.82rem;">€</span>
    </div>
  `).join('');
}

function saveCVChanges() {
  COACH_CV.prenom = document.getElementById('cvPrenom').value;
  COACH_CV.nom = document.getElementById('cvNom').value;
  COACH_CV.badge = document.getElementById('cvBadge').value;
  COACH_CV.tagline = document.getElementById('cvTagline').value;
  COACH_CV.tel = document.getElementById('cvTel').value;
  COACH_CV.email = document.getElementById('cvEmail').value;
  COACH_CV.loc = document.getElementById('cvLoc').value;
  localStorage.setItem('mehdi_cv', JSON.stringify(COACH_CV));

  // Apply to public page
  const heroName = document.querySelector('.hero-name');
  if (heroName) heroName.innerHTML = COACH_CV.prenom + ' <span>' + COACH_CV.nom + '</span>';
  const heroBadge = document.querySelector('.hero-badge');
  if (heroBadge) heroBadge.textContent = COACH_CV.badge;
  const heroTagline = document.querySelector('.hero-tagline');
  if (heroTagline) heroTagline.textContent = COACH_CV.tagline;
  const footerName = document.querySelector('.footer-name');
  if (footerName) footerName.textContent = COACH_CV.prenom + ' ' + COACH_CV.nom;

  // Rebuild credential pills
  const heroCredsEl = document.querySelector('.hero-credentials');
  if (heroCredsEl) {
    heroCredsEl.innerHTML = COACH_CV.creds.map(cr =>
      `<div class="cred-pill"><i class="fas ${cr.icon}"></i>${cr.text}</div>`
    ).join('');
  }

  // Update packs on public page
  if (window.PACKS_DATA) {
    document.querySelectorAll('.pack-card').forEach((card, i) => {
      if (window.PACKS_DATA[i]) {
        card.dataset.price = window.PACKS_DATA[i].price;
        const priceEl = card.querySelector('.pack-price');
        if (priceEl) priceEl.innerHTML = `<span>€</span>${window.PACKS_DATA[i].price}`;
      }
    });
  }

  showToast('✓ CV mis à jour sur la page publique !');
}

// ── SLOTS MANAGER ──
function renderSlotsManager() {
  if (!SLOTS_STATE) {
    SLOTS_STATE = {};
    Object.entries(SCHEDULE).forEach(([day, times]) => {
      SLOTS_STATE[day] = {};
      times.forEach(slot => { SLOTS_STATE[day][slot.time] = slot.available !== false; });
    });
  }
  const container = document.getElementById('slotsManagerGrid');
  container.innerHTML = Object.entries(SCHEDULE).map(([day, times]) => `
    <div class="slot-day-section">
      <div class="slot-day-title">${day.charAt(0).toUpperCase()+day.slice(1)}</div>
      ${times.map(slot => {
        const isOn = SLOTS_STATE[day]?.[slot.time] !== false;
        return `
          <div class="slot-toggle-row" onclick="toggleSlot('${day}','${slot.time}')">
            <div class="slot-toggle ${isOn?'on':'off'}" id="toggle_${day}_${slot.time.replace(':','')}" ></div>
            <div class="slot-label">${slot.time}</div>
            <div class="slot-type-label">${slot.type||''}</div>
          </div>
        `;
      }).join('')}
    </div>
  `).join('');
}

function toggleSlot(day, time) {
  if (!SLOTS_STATE[day]) SLOTS_STATE[day] = {};
  SLOTS_STATE[day][time] = !SLOTS_STATE[day][time];
  const id = 'toggle_' + day + '_' + time.replace(':','');
  const el = document.getElementById(id);
  if (el) { el.classList.toggle('on'); el.classList.toggle('off'); }
}

function saveSlots() {
  localStorage.setItem('mehdi_slots_state', JSON.stringify(SLOTS_STATE));
  // Re-render public slots with new availability
  Object.entries(SLOTS_STATE).forEach(([day, times]) => {
    Object.entries(times).forEach(([time, avail]) => {
      const idx = SCHEDULE[day]?.findIndex(s => s.time === time);
      if (idx !== undefined && idx >= 0) SCHEDULE[day][idx].available = avail;
    });
  });
  renderSlots();
  showToast('✓ Créneaux mis à jour !');
}

// ── TOAST ──
function showToast(msg) {
  const t = document.getElementById('toastMsg');
  t.textContent = msg;
  t.classList.add('show');
  setTimeout(() => t.classList.remove('show'), 3000);
}

// ── APPLY SAVED CV ON LOAD ──
(function applySavedCV() {
  if (!COACH_CV) return;
  const heroName = document.querySelector('.hero-name');
  if (heroName) heroName.innerHTML = COACH_CV.prenom + ' <span>' + COACH_CV.nom + '</span>';
  const heroBadge = document.querySelector('.hero-badge');
  if (heroBadge) heroBadge.textContent = COACH_CV.badge;
  const heroTagline = document.querySelector('.hero-tagline');
  if (heroTagline) heroTagline.textContent = COACH_CV.tagline;
  const footerName = document.querySelector('.footer-name');
  if (footerName) footerName.textContent = COACH_CV.prenom + ' ' + COACH_CV.nom;
  const heroCredsEl = document.querySelector('.hero-credentials');
  if (heroCredsEl && COACH_CV.creds?.length) {
    heroCredsEl.innerHTML = COACH_CV.creds.map(cr =>
      `<div class="cred-pill"><i class="fas ${cr.icon}"></i>${cr.text}</div>`
    ).join('');
  }
})();

// ── INIT ──
createParticles();
setupHealthToggles();
setupIMCCalc();
renderSlots();
</script>
</body>
</html>
